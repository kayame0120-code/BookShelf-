status: patched (C-4適用済み)

# 検品表 03 — レビュー・お気に入り・いいね

**対象発注書**: `docs/02_発注書/03_レビューお気に入りいいね.md`
**出力先**: `docs/04_検品結果/03_レビューお気に入りいいね_結果.md`

## 検品セッションの実施条件

- 実装を行ったCCセッションとは別の、文脈ゼロの新規セッションで実施する。
- 渡してよいのは①`CLAUDE.md`（自動読込） ②本検品表と対の発注書 ③実装済みコードの3点のみ。
- 全行に判定（YES/NO）と根拠を必ず添える。根拠なしのYESは無効。
- 1行でもNOがあれば走行③は不合格。走行④に進まない。

## 検品前に一度だけ実行するコマンド

```bash
git branch --show-current
git log --oneline -5
sail artisan route:list --path=reviews
sail artisan route:list --path=favorites
sail bin pint --test
```

---

## A. ルーティング

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| A-1 | `reviews.store` `reviews.edit` `reviews.update` `reviews.destroy` `reviews.like` の5本が存在する | `route:list --path=reviews` | 差し戻し |
| A-2 | `favorites.index` `favorites.toggle` の2本が存在する | `route:list --path=favorites` | 差し戻し |
| A-3 | 専用の `LikeController` が作られておらず、いいねが `ReviewController@like` に統合されている | ソース確認 | 差し戻し |
| A-4 | `FavoriteController` が独立して存在する（`ReviewController`に統合されていない） | ソース確認 | 差し戻し |
| A-5 | 全7ルートが `auth` ミドルウェア配下にある | ソース確認 | 差し戻し |

## B. レビュー投稿・編集・削除

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| B-1 | `rating`必須・`comment`任意で投稿でき、`books.show`へ遷移し「レビューを投稿しました」が表示される | 実機 | 差し戻し |
| B-2 | `rating`未入力・範囲外（0や6）でエラーが表示される。エラー文言が本書§4と一致する | 実機 | 差し戻し |
| B-3 | 投稿者本人は編集・削除ができ、それ以外は403になる | 実機（2ユーザーで確認） | 差し戻し |
| B-4 | レビュー編集画面で `$review->book->title` が正しく表示される（削除済み書籍のレビューでも落ちない） | 実機（削除済み書籍のレビューを編集） | 差し戻し |
| B-5 | レビュー削除時、紐づく `review_likes` が連動して削除される（cascade） | DB確認（削除前後の`review_likes`件数） | 差し戻し |
| B-6 | 削除後のリダイレクト先が正しく解決される（`$review->book`を削除前に取得している） | ソース確認・実機 | 差し戻し |

## C. いいねトグル

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| C-1 | 「いいね」ボタンでON/OFFが `review_likes` に正しく反映される | 実機・DB確認 | 差し戻し |
| C-2 | いいね操作でflashメッセージが表示されない | 実機 | 差し戻し |
| C-3 | 参照先の書籍が削除済みのレビューでも、いいね操作が引き続き可能である | 実機（削除済み書籍のレビューにいいね） | 差し戻し |
| C-4 | 未ログインでいいねを押すと `/login` へリダイレクトされる | Featureテストで`actingAs()`を使わずに`post(route('reviews.like',$review))`を実行し`assertRedirect('/login')`で確認（生curlはCSRF検証が`auth`より先行するため419になり検証不能。`検品表02_B2・検品表03_C4_修正パッチ.md`により2026-09-03付で確定） | 差し戻し |

## D. お気に入りトグル・一覧

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| D-1 | 未登録の書籍でボタンを押すと `favorites` にレコードが作成される | 実機・DB確認 | 差し戻し |
| D-2 | 登録済みの書籍で再度押すと `favorites` からレコードが削除される | 実機・DB確認 | 差し戻し |
| D-3 | お気に入り操作でflashメッセージが表示されない | 実機 | 差し戻し |
| D-4 | 未ログインで `/favorites` にアクセスすると `/login` へリダイレクトされる | 実機 | 差し戻し |
| D-5 | お気に入り一覧に自分が登録した書籍のみが表示される（他ユーザーの分は出ない） | 実機（2ユーザーで確認） | 差し戻し |
| D-6 | お気に入り一覧の並び順が `books.created_at` 降順である | 実機 | 差し戻し |

## E. 削除済み書籍との関係

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| E-1 | 論理削除済みの書籍がお気に入り一覧から表示されない | 実機 | 差し戻し |
| E-2 | 書籍削除後も `favorites` レコードが保持される（消えない） | DB確認 | 差し戻し |
| E-3 | 書籍を復元するとお気に入り一覧に再び現れる | 実機 | 差し戻し |

## F. コード品質・スコープ厳守

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| F-1 | `sail bin pint --test` が「No fixable issues were found」 | コマンド出力 | 差し戻し |
| F-2 | バリデーションが `StoreReviewRequest` / `UpdateReviewRequest` に分離されている | ソース確認 | 差し戻し |
| F-3 | 認可が `ReviewPolicy` に実装され、コントローラーで `$this->authorize()` が呼ばれている | ソース確認 | 差し戻し |
| F-4 | `ReviewPolicy` の判定に書籍の削除状態（`$book->trashed()`）が**含まれていない** | ソース確認（`BookPolicy`との混同がないか） | 差し戻し |
| F-5 | 発注書に明記されていない独自ロジックが追加されていない | ソース確認・実機 | 差し戻し |
| F-6 | migration が変更されていない | `git diff main -- database/migrations/` | 差し戻し |
| F-7 | `QUESTIONS.md` に走行③由来の未解消行がない、または全て記録されている | `QUESTIONS.md` 確認 | 差し戻し |
