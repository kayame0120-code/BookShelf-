status: draft

# 検品表 04 — ジャンル・ランキング

**対象発注書**: `docs/02_発注書/04_ジャンルランキング.md`
**出力先**: `docs/04_検品結果/04_ジャンルランキング_結果.md`

## 検品セッションの実施条件

- 実装を行ったCCセッションとは別の、文脈ゼロの新規セッションで実施する。
- 渡してよいのは①`CLAUDE.md`（自動読込） ②本検品表と対の発注書 ③実装済みコードの3点のみ。
- 全行に判定（YES/NO）と根拠を必ず添える。根拠なしのYESは無効。
- 1行でもNOがあれば走行④は不合格。走行⑤に進まない。

## 検品前に一度だけ実行するコマンド

```bash
git branch --show-current
git log --oneline -5
sail artisan route:list --path=genres
sail artisan route:list --path=ranking
sail bin pint --test
```

---

## A. ルーティング

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| A-1 | `genres.index/create/store/show/edit/update/destroy`の7本が存在する | `route:list --path=genres` | 差し戻し |
| A-2 | `ranking.index`が存在し、`auth`ミドルウェアが付いていない | `route:list --path=ranking` | 差し戻し |
| A-3 | `App\Policies\GenrePolicy`が存在しない | ファイル確認（`ls app/Policies/`） | 差し戻し |

## B. ジャンル一覧・詳細

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| B-1 | 一覧に各ジャンルの書籍数が表示される（`books_count`） | 実機 | 差し戻し |
| B-2 | 書籍数は削除済み書籍を含まない生存書籍のみでカウントされている | 実機（削除済み書籍しか紐付かないジャンルで0冊表示を確認） | 差し戻し |
| B-3 | ジャンル詳細に紐づく書籍が10件ページネーションで表示される | 実機 | 差し戻し |
| B-4 | 詳細画面の各書籍タグで、表示中ジャンルと一致するものだけ色が変わる | 実機 | 差し戻し |

## C. 登録・編集

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| C-1 | `name`未入力でエラーが表示される | 実機 | 差し戻し |
| C-2 | `name`重複でエラーが表示される（「このジャンル名は既に登録されています」） | 実機 | 差し戻し |
| C-3 | 編集は所有者概念がなく、ログイン済みであれば誰でも実行できる（作成者でなくても編集できる） | 実機（別ユーザーで編集） | 差し戻し |
| C-4 | 編集時の一意性チェックが自身のレコードを除外する | 実機 | 差し戻し |

## D. 削除制限（二重防御）

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| D-1 | 紐づく書籍が1件でも存在する場合、error flash「このジャンルに紐づく書籍が存在するため削除できません」とともに削除が失敗し、レコードが残る | 実機 | 差し戻し |
| D-2 | 紐づく書籍が存在しない場合は削除でき、「ジャンルを削除しました」が表示される | 実機 | 差し戻し |
| D-3 | アプリ側チェックが `withTrashed()` 付きである（削除済み書籍にのみ紐づくジャンルでも削除がブロックされ、500エラーにならない） | 実機（該当ジャンルを作って確認） | 差し戻し |
| D-4 | 二重防御 — アプリ側チェックを迂回（`GenreController@destroy`を経由しないtinker等での直接delete）してもDB制約`restrictOnDelete()`で削除が防止される | `sail artisan tinker`で紐付きのあるジャンルを直接`->delete()`し例外が出ることを確認 | 差し戻し |

## E. ランキング

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| E-1 | 平均評価（`reviews_avg_rating`）の降順でTOP10が表示される | 実機 | 差し戻し |
| E-2 | レビューが1件もない書籍はランキング対象外である | 実機 | 差し戻し |
| E-3 | 論理削除済みの書籍がランキングの集計・表示対象から除外される | 実機（書籍を削除して確認） | 差し戻し |
| E-4 | 削除した書籍を復元するとランキングに再び現れる | 実機 | 差し戻し |
| E-5 | 未ログインでも `/ranking` を閲覧できる | 実機 | 差し戻し |
| E-6 | 変数名が `$rankedBooks` である（`$books`ではない） | ソース確認 | 差し戻し |
| E-7 | 平均評価が本文で小数第2位、右バッジで小数第1位の両方で表示される | 実機 | 差し戻し |

## F. コード品質・スコープ厳守

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| F-1 | `sail bin pint --test` が「No fixable issues were found」 | コマンド出力 | 差し戻し |
| F-2 | バリデーションが `StoreGenreRequest` / `UpdateGenreRequest` に分離されている | ソース確認 | 差し戻し |
| F-3 | ランキングのクエリが `withAvg`/`withCount`/`whereHas`で構成され、`with('reviews')`で全件ロードしていない | ソース確認 | 差し戻し |
| F-4 | 発注書に明記されていない独自ロジックが追加されていない | ソース確認・実機 | 差し戻し |
| F-5 | migration が変更されていない | `git diff main -- database/migrations/` | 差し戻し |
| F-6 | `QUESTIONS.md` に走行④由来の未解消行がない、または全て記録されている | `QUESTIONS.md` 確認 | 差し戻し |
