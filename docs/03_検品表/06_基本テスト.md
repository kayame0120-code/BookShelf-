status: draft

# 検品表 06 — 基本テスト

**対象発注書**: `docs/02_発注書/06_基本テスト.md`
**出力先**: `docs/04_検品結果/06_基本テスト_結果.md`

## 検品セッションの実施条件

- 実装を行ったCCセッションとは別の、文脈ゼロの新規セッションで実施する。
- 渡してよいのは①`CLAUDE.md`（自動読込） ②本検品表と対の発注書 ③実装済みコード（テストコード含む）の3点のみ。
- 全行に判定（YES/NO）と根拠を必ず添える。根拠なしのYESは無効。
- 1行でもNOがあれば走行⑥は不合格。基本一括検品（実機）に進まない。

## 検品前に一度だけ実行するコマンド（出力を結果mdに貼る）

```bash
git branch --show-current
git log --oneline -5
sail artisan test
sail artisan test --coverage
sail bin pint --test
```

---

## A. 実行結果

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| A-1 | `sail artisan test` が全件PASSする（FAILが0件） | コマンド出力 | 差し戻し |
| A-2 | `sail artisan test --coverage` の基本機能カバレッジが60%を超える | コマンド出力 | 差し戻し |
| A-3 | テスト実行のたびに結果が変わらない（フレーキーでない）。同一コマンドを2回実行して結果が一致する | 2回実行して比較 | 差し戻し |

## B. テストファイルの存在と観点網羅

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| B-1 | `tests/Feature/ScreenAccessTest.php` が存在し、F-A1〜F-A4の4観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-2 | `tests/Feature/AuthTest.php` が存在し、F-A5〜F-A9の5観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-3 | `tests/Unit/BookTest.php` が存在し、U-B1〜U-B5の5観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-4 | `tests/Feature/BookCrudTest.php` が存在し、F-B1〜F-B13の13観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-5 | `tests/Unit/ReviewTest.php` が存在し、U-R1〜U-R4の4観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-6 | `tests/Feature/ReviewTest.php` が存在し、F-R1〜F-R5の5観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-7 | `tests/Feature/ReviewLikeTest.php` が存在し、F-L1〜F-L2の2観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-8 | `tests/Unit/FavoriteTest.php` が存在し、U-F1に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-9 | `tests/Feature/FavoriteTest.php` が存在し、F-F1〜F-F5の5観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-10 | `tests/Unit/GenreTest.php` が存在し、U-G1に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-11 | `tests/Feature/GenreTest.php` が存在し、F-G1〜F-G6の6観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-12 | `tests/Feature/RankingTest.php` が存在し、F-K1〜F-K4の4観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-13 | `tests/Feature/Api/BookApiTest.php` が存在し、F-P1〜F-P10の10観点に対応するテストメソッドがある | ソース確認 | 差し戻し |
| B-14 | `tests/Feature/SeederTest.php` が存在し、F-P11〜F-P12に対応するテストメソッドがある（推奨扱いだが、存在しない場合はNOとして記録し、片倉の裁定を仰ぐ） | ソース確認 | 記録の上で片倉に確認 |

## C. テストの質（形骸化していないか）

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| C-1 | 認可系テスト（F-B4/F-B5/F-B11/F-R2/F-R3/F-G3）で「本人」と「それ以外」が明確に別の`User`インスタンスとして扱われている（同一ユーザーで403判定を回避していない） | ソース確認 | 差し戻し |
| C-2 | SoftDeletes系テスト（F-B6〜F-B13/F-K3）が`assertDatabaseHas`等でDBレベルの保持を確認しており、画面表示の確認だけで済ませていない | ソース確認 | 差し戻し |
| C-3 | 各テストメソッドが該当観点に無関係なアサーションだけで「グリーン」になっていない（例: レスポンスコードだけ見てレスポンスボディの構造を見ていない、等の手抜きがない） | ソース確認（抜き取りで3件） | 差し戻し |
| C-4 | テストで見つかった実装不具合が、この走行内で無断修正されていない（`QUESTIONS.md`に記録されているか、実装側コミットに走行⑥のコミットが混ざっていないか） | `git log`で走行②〜⑤対象ファイルへの変更がないか確認 | 差し戻し |

## D. コード品質・スコープ厳守

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| D-1 | `sail bin pint --test` が「No fixable issues were found」 | コマンド出力 | 差し戻し |
| D-2 | migration・実装コード（Controller/Model/Policy/FormRequest）が変更されていない（`git diff main --stat`でtests/配下以外に差分がない） | `git diff` | 差し戻し |
| D-3 | `QUESTIONS.md` に走行⑥由来の未解消行がない、または全て記録されている | `QUESTIONS.md` 確認 | 差し戻し |
