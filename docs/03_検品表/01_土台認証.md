status: draft

# 検品表 01 — 土台＋認証

**対象発注書**: `docs/02_発注書/01_土台認証.md`
**出力先**: `docs/04_検品結果/01_土台認証_結果.md`

## 検品セッションの実施条件

- **実装を行ったCCセッションとは別の、文脈ゼロの新規セッションで実施する。**
- 検品セッションに渡してよいのは次の3点のみ: ①`CLAUDE.md`（自動読込） ②本検品表と対の発注書 ③実装済みコード。実装時の会話ログ・設計の経緯は渡さない。
- 全行に「判定（YES / NO）」と「根拠（コマンド出力の該当行、または画面で確認した内容）」を必ず添える。**根拠なしのYESは無効。**
- 1行でもNOがあれば走行①は不合格。走行②に進まない。

## 検品前に一度だけ実行するコマンド（出力を結果mdに貼る）

```bash
git branch --show-current
git log --oneline -5
sail artisan migrate:fresh
sail exec mysql mysql -usail -ppassword laravel -e "SHOW TABLES;"
for t in users genres books reviews book_genre favorites review_likes; do
  sail exec mysql mysql -usail -ppassword laravel -e "SHOW CREATE TABLE $t\G"
done
sail artisan route:list
sail bin pint --test
```

---

## A. ブランチ・コミット規律

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| A-1 | 作業ブランチが `feature/01-foundation-auth` である | `git branch --show-current` の出力が一致する | 差し戻し |
| A-2 | `main` に走行①のコミットが直接入っていない | `git log --oneline -5 main` に走行①のコミットが含まれない | 差し戻し |
| A-3 | 全コミットメッセージが「何をしたか」を日本語または英語で述べている（`wip`・`fix`単独・`update` 等の内容不明なものがない） | `git log --oneline -5` | 差し戻し（メッセージ書き直し） |

## B. テーブル構成

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| B-1 | `sail artisan migrate:fresh` がエラーなく完了する | コマンド出力に `Exception` / `SQLSTATE` がない | 差し戻し |
| B-2 | 存在するテーブルが `migrations` / `users` / `password_reset_tokens` / `failed_jobs` / `genres` / `books` / `reviews` / `book_genre` / `favorites` / `review_likes` の **10個ちょうど** である | `SHOW TABLES;` の出力 | 差し戻し |
| B-3 | `personal_access_tokens` テーブルが存在しない | 同上 | 差し戻し（マイグレーションファイル削除） |
| B-4 | `sessions` テーブルが存在しない | 同上 | 差し戻し |
| B-5 | `users` に `two_factor_secret` / `two_factor_recovery_codes` / `two_factor_confirmed_at` の3カラムが存在する | `SHOW CREATE TABLE users` | 差し戻し |
| B-6 | `users` の既存カラム（id / name / email / email_verified_at / password / remember_token / created_at / updated_at）が Laravel 標準のまま改変されていない | `SHOW CREATE TABLE users` ＋ `git diff main -- database/migrations/2014_10_12_000000_create_users_table.php` が空 | 差し戻し |

## C. カラム定義

| No. | 判定条件（YES/NO） | 確認方法（`SHOW CREATE TABLE` の該当行） | 違反時処置 |
|---|---|---|---|
| C-1 | `genres.name` が `varchar(255)` NOT NULL かつ UNIQUE キーを持つ | `genres` | 差し戻し |
| C-2 | `books.title` `books.author` が `varchar(255)` NOT NULL | `books` | 差し戻し |
| C-3 | `books.isbn` が `varchar(13)` NOT NULL かつ UNIQUE キーを持つ | `books` | 差し戻し |
| C-4 | `books.published_date` が `date` NOT NULL | `books` | 差し戻し |
| C-5 | `books.description` が `text` NULL許可 | `books` | 差し戻し |
| C-6 | `books.image_url` が `varchar(255)` NULL許可 | `books` | 差し戻し |
| C-7 | `books.deleted_at` が `timestamp` NULL許可で存在する | `books` | 差し戻し |
| C-8 | `books.user_id` が `bigint unsigned` NOT NULL | `books` | 差し戻し |
| C-9 | `reviews.rating` が `tinyint unsigned` NOT NULL で、CHECK制約が付いていない | `reviews` | 差し戻し |
| C-10 | `reviews.comment` が `text` NULL許可 | `reviews` | 差し戻し |
| C-11 | `book_genre` の主キーが `(book_id, genre_id)` の複合主キーである | `book_genre` の `PRIMARY KEY` 行 | 差し戻し |
| C-12 | `favorites` の主キーが `(user_id, book_id)` の複合主キーである | `favorites` の `PRIMARY KEY` 行 | 差し戻し |
| C-13 | `review_likes` の主キーが `(user_id, review_id)` の複合主キーである | `review_likes` の `PRIMARY KEY` 行 | 差し戻し |
| C-14 | `book_genre` / `favorites` / `review_likes` の3テーブルに `created_at` `updated_at` カラムが存在しない | 各 `SHOW CREATE TABLE` | 差し戻し |
| C-15 | `book_genre` / `favorites` / `review_likes` に `id` カラムが存在しない | 各 `SHOW CREATE TABLE` | 差し戻し |

## D. 外部キーの削除挙動

`SHOW CREATE TABLE` の `CONSTRAINT ... FOREIGN KEY ... ON DELETE ...` 行で判定する。`ON DELETE` の記載がない場合は `RESTRICT` 相当（MySQL既定は NO ACTION = RESTRICT）であり、YESとしてよい。

| No. | 判定条件（YES/NO） | 対象テーブル | 違反時処置 |
|---|---|---|---|
| D-1 | `books.user_id` → `users.id` が RESTRICT | `books` | 差し戻し |
| D-2 | `reviews.user_id` → `users.id` が RESTRICT | `reviews` | 差し戻し |
| D-3 | `reviews.book_id` → `books.id` が CASCADE | `reviews` | 差し戻し |
| D-4 | `book_genre.book_id` → `books.id` が CASCADE | `book_genre` | 差し戻し |
| D-5 | `book_genre.genre_id` → `genres.id` が RESTRICT | `book_genre` | 差し戻し |
| D-6 | `favorites.user_id` → `users.id` が RESTRICT | `favorites` | 差し戻し |
| D-7 | `favorites.book_id` → `books.id` が CASCADE | `favorites` | 差し戻し |
| D-8 | `review_likes.user_id` → `users.id` が RESTRICT | `review_likes` | 差し戻し |
| D-9 | `review_likes.review_id` → `reviews.id` が CASCADE | `review_likes` | 差し戻し |

## E. モデル（tinker で実行して判定）

以下を `sail artisan tinker` に貼り、各行の出力を結果mdに記録する。

```php
$u = \App\Models\User::factory()->create();
$g = \App\Models\Genre::factory()->create();
$b = \App\Models\Book::factory()->create(['user_id' => $u->id]);
$b->genres()->attach($g->id);
$r = \App\Models\Review::factory()->create(['book_id' => $b->id, 'user_id' => $u->id, 'rating' => 4]);
$r->likedByUsers()->attach($u->id);
$u->favoriteBooks()->attach($b->id);

// E-1〜E-9
[$b->user->id, $b->genres->count(), $b->reviews->count(), $g->books->count()];
[$r->user->id, $r->book->id, $r->likedByUsers->count()];
[$u->books->count(), $u->reviews->count(), $u->favoriteBooks->count(), $u->likedReviews->count()];

// E-10 平均評価（集約属性）
\App\Models\Book::withAvg('reviews','rating')->withCount('reviews')->find($b->id)->only(['reviews_avg_rating','reviews_count']);

// E-11 レビュー0件の書籍
$b2 = \App\Models\Book::factory()->create(['user_id' => $u->id]);
\App\Models\Book::withAvg('reviews','rating')->find($b2->id)->reviews_avg_rating;

// E-12 published_date の型（DBから読み直した状態で判定する）
$fresh = \App\Models\Book::find($b->id);
[gettype($fresh->published_date), (string) $fresh->published_date];

// E-13/E-14 SoftDelete
$b2->delete();
[\App\Models\Book::find($b2->id), \App\Models\Book::withTrashed()->find($b2->id)?->id];

// E-15 レビューが残ること
$b->delete();
\App\Models\Review::where('book_id', $b->id)->count();
```

| No. | 判定条件（YES/NO） | 期待される出力 | 違反時処置 |
|---|---|---|---|
| E-1 | `Book::user()` が定義され、登録者を返す | `$b->user->id` が `$u->id` と一致 | 差し戻し |
| E-2 | `Book::genres()` が `book_genre` 経由で取得できる | `1` | 差し戻し |
| E-3 | `Book::reviews()` が取得できる | `1` | 差し戻し |
| E-4 | `Genre::books()` が `book_genre` 経由で取得できる | `1` | 差し戻し |
| E-5 | `Review::user()` が取得できる | `$u->id` と一致 | 差し戻し |
| E-6 | `Review::book()` が取得できる | `$b->id` と一致 | 差し戻し |
| E-7 | `Review::likedByUsers()` が `review_likes` 経由で取得できる（**メソッド名がこの綴りであること**） | `1` | 差し戻し |
| E-8 | `User::books()` / `User::reviews()` が取得できる | ともに `1` | 差し戻し |
| E-9 | `User::favoriteBooks()` / `User::likedReviews()` が取得できる（**メソッド名がこの綴りであること**） | ともに `1` | 差し戻し |
| E-10 | `withAvg('reviews','rating')` が `reviews_avg_rating`、`withCount('reviews')` が `reviews_count` を返す | `['reviews_avg_rating' => 4.0, 'reviews_count' => 1]` 相当 | 差し戻し |
| E-11 | レビュー0件の書籍で `reviews_avg_rating` が `null` になる | `null` | 差し戻し |
| E-12 | `published_date` が文字列で、`YYYY-MM-DD` 形式のまま返る（Carbon にキャストされていない） | `"string"` かつ `"2012-06-23"` のような10文字 | 差し戻し |
| E-13 | `Book::find()` が削除済みレコードを返さない | `null` | 差し戻し |
| E-14 | `Book::withTrashed()->find()` が削除済みレコードを返す | `$b2->id` と一致 | 差し戻し |
| E-15 | 書籍を削除しても `reviews` が物理削除されず `book_id` を保持したまま残る | `1` | **停止**（削除連動 土台設計確定書に反する。設計に戻す） |
| E-16 | `app/Models/Book.php` に `SoftDeletes` トレイトが use されている | ファイル目視 | 差し戻し |
| E-17 | `Book` に `getAverageRatingAttribute` 等の平均評価アクセサが**存在しない** | `grep -n "AverageRating\|average_rating" app/Models/Book.php` が0件 | 差し戻し |
| E-18 | `Book` の `$casts` に `published_date` が**含まれていない** | `grep -n "published_date" app/Models/Book.php` が `$fillable` の1件のみ | 差し戻し |
| E-19 | モデルのメソッドに戻り値型宣言・PHPDoc が付いていない（走行⑬のスコープ） | 4モデルを目視 | 差し戻し |

## F. Fortify の設定

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| F-1 | `composer.json` の `require` に `laravel/fortify` が入っており、`composer.lock` の解決バージョンが `v1.x` である | `grep -n fortify composer.json` ／ `composer.lock` 内の `"name": "laravel/fortify"` ブロックの `version` フィールド | 差し戻し |
| F-2 | `config/app.php` の `providers` に `App\Providers\FortifyServiceProvider::class` がある | `grep -n FortifyServiceProvider config/app.php` | 差し戻し |
| F-3 | `RouteServiceProvider::HOME` が `'/books'` である | `grep -n "const HOME" app/Providers/RouteServiceProvider.php` | 差し戻し |
| F-4 | `config/fortify.php` の `features` が `Features::registration()` の1要素のみである | `config/fortify.php` 目視 | 差し戻し |
| F-5 | `FortifyServiceProvider` に `Fortify::loginView` と `Fortify::registerView` の登録があり、返すビューが `auth.login` / `auth.register` である | ファイル目視 | 差し戻し |
| F-6 | `FortifyServiceProvider` に `RateLimiter::for('two-factor', ...)` が残っていない | `grep -n "two-factor" app/Providers/FortifyServiceProvider.php` が0件 | 差し戻し |
| F-7 | `app/Actions/Fortify/` に `CreateNewUser.php` **のみ**が存在する（`UpdateUserProfileInformation` / `UpdateUserPassword` / `ResetUserPassword` / `PasswordValidationRules` が削除済み） | `ls app/Actions/Fortify/` | 差し戻し |
| F-8 | `CreateNewUser` のルールが `name: required,string,max:255` / `email: required,email,max:255,unique:users,email` / `password: required,string,min:8,confirmed` である | ファイル目視 | 差し戻し |
| F-9 | `CreateNewUser` の `messages()` 相当に、要件シート シート8「会員登録」の確定文言8種がそのまま入っている | ファイル目視 | 差し戻し |
| F-10 | 独自の `LoginRequest` を作っていない（Fortify 標準を使っている） | `find app -name "LoginRequest.php"` が0件 | 差し戻し |
| F-11 | `route:list` に `GET/POST /register`・`GET/POST /login`・`POST /logout` の5本が存在する | `sail artisan route:list` | 差し戻し |
| F-12 | `route:list` に `password.request` / `password.reset` / `verification.notice` / `two-factor` 系のルートが存在しない | 同上 | 差し戻し |
| F-13 | `route:list` に books / genres / reviews / favorites / ranking / api 系のルートが**存在しない**（スコープ超過の検出） | 同上 | 差し戻し（先取り実装を撤去） |

## G. 実機動作（ブラウザで操作して判定）

前提: `sail up -d` と `sail npm run dev` が動作中。`sail artisan migrate:fresh` 直後の空DBから開始する。

| No. | 判定条件（YES/NO） | 操作 | 違反時処置 |
|---|---|---|---|
| G-1 | `/register` が崩れずに表示され、「お名前」「メールアドレス」「パスワード」「パスワード確認」の4項目と「登録」「ログイン」ボタンがある | ブラウザで `http://localhost/register` | 差し戻し |
| G-2 | 4項目すべて空のまま「登録」を押すと会員登録画面に戻り、「お名前を入力してください」「メールアドレスを入力してください」「パスワードを入力してください」が表示される | 実機操作 | 差し戻し |
| G-3 | メールアドレスに `abc` と入力して登録すると「メールアドレスの形式が正しくありません」が表示される | 実機操作 | 差し戻し |
| G-4 | パスワードとパスワード確認を食い違わせると「パスワード確認が一致しません」が表示される | 実機操作 | 差し戻し |
| G-5 | パスワードを7文字にすると「パスワードは8文字以上で入力してください」が表示される | 実機操作 | 差し戻し |
| G-6 | エラー時にお名前・メールアドレスの入力値が保持されている（`old()`） | 実機操作 | 差し戻し |
| G-7 | 正しい4項目で登録すると `302` が返り、`Location` ヘッダが `/books` である | ブラウザの開発者ツール Network タブ、または `curl -i` | 差し戻し |
| G-8 | 登録後、`users` テーブルにレコードが1件増えており、`password` が平文でなくハッシュ値である | `sail exec mysql mysql -usail -ppassword laravel -e "SELECT id,name,email,LEFT(password,7) FROM users;"` | 差し戻し |
| G-9 | 登録直後にログイン状態になっている（自動ログイン） | `/login` にアクセスすると `302` `/books` に飛ばされる | 差し戻し |
| G-10 | 同じメールアドレスで再度登録すると「このメールアドレスは既に登録されています」が表示される | 実機操作（一度ログアウトしてから） | 差し戻し |
| G-11 | `/login` が崩れずに表示され、「メールアドレス」「パスワード」の2項目と「ログイン」「会員登録」ボタンがある | ブラウザで `http://localhost/login` | 差し戻し |
| G-12 | 誤ったパスワードでログインするとログイン画面に戻り、**「メールアドレスまたはパスワードが正しくありません」** が表示される（英語メッセージでない） | 実機操作 | 差し戻し |
| G-13 | 正しいメール・パスワードでログインすると `302` が返り、`Location` ヘッダが `/books` である | 開発者ツール Network タブ | 差し戻し |
| G-14 | ログイン済みの状態で `/register` にアクセスすると `302` `/books` にリダイレクトされる | 実機操作 | 差し戻し |
| G-15 | `POST /logout` ルートが `Laravel\Fortify\Http\Controllers\AuthenticatedSessionController@destroy` に割り当てられており、`LogoutResponse` の独自バインドがない（Fortify標準の遷移が生きている） | `sail artisan route:list --path=logout` ＋ `grep -rn "LogoutResponse" app/ config/` が0件。※ログアウトボタンを持つ画面（`layouts/navigation`）は走行②で app-layout を使う画面が実装されるまで到達できないため、**実機でのログアウト動作確認は検品表02へ申し送る** | 差し戻し |
| G-16 | `/` が Laravel 標準の welcome 画面としてエラーなく表示される | ブラウザで `http://localhost/` | 差し戻し |
| G-17 | `/books` が 404 になる（走行①時点では未実装が正しい。500やRouteNotFoundExceptionではないこと） | ブラウザで `http://localhost/books` | 差し戻し |

## H. コード品質

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| H-1 | `sail bin pint --test` が `No fixable issues were found` を出力する | コマンド出力 | 差し戻し |
| H-2 | Bladeモック（`resources/views/`）が frozen 宣言後に改変されていない | モック basic ブランチを一時ディレクトリへ再clone → `diff -r <一時clone>/resources/views resources/views` の出力が空（`docs/03_検品表/検品表01_H2修正パッチ.md` により差し替え） | **停止**（モックは frozen。改変は正本違反） |
| H-3 | `composer.json` に `laravel-lang/*` 系パッケージが入っていない | `grep -n "laravel-lang" composer.json` が0件 | **停止**（シート4の明示的禁止） |
| H-4 | 設定値（DB接続情報・パスワード等）がコードに直書きされていない | 追加・変更したファイルの目視 | 差し戻し |
| H-5 | クラス名がアッパーキャメル、メソッド・変数が camelCase、マイグレーションファイル名が snake_case、テーブル名が snake_case 複数形（`book_genre` は Laravel 規約のアルファベット順単数形連結のため例外的に正しい） | 追加ファイルの目視 | 差し戻し |
| H-6 | `a` `x` 等の無意味な変数名・ローマ字命名がない | 追加ファイルの目視 | 差し戻し |

## I. 走行完了条件

| No. | 判定条件（YES/NO） | 確認方法 | 違反時処置 |
|---|---|---|---|
| I-1 | `QUESTIONS.md` に走行①で追記された行がない。ある場合は片倉が裁定済みで、解消済みとして消されている | `QUESTIONS.md` の内容 | 走行②に進まず裁定を先に行う |
| I-2 | A〜H の全行が YES である | 本表 | 不合格。差し戻し1ラウンドで全指摘を一括修正し、B以降を再実行する |

---

## 判定サマリ記入欄（結果mdにコピーして使う）

| 区分 | 行数 | YES | NO |
|---|---|---|---|
| A ブランチ・コミット | 3 | | |
| B テーブル構成 | 6 | | |
| C カラム定義 | 15 | | |
| D 外部キー | 9 | | |
| E モデル | 19 | | |
| F Fortify | 13 | | |
| G 実機動作 | 17 | | |
| H コード品質 | 6 | | |
| I 走行完了条件 | 2 | | |
| **合計** | **90** | | |

**総合判定**: 合格 / 不合格
**対象コミットID**:
**CCの解釈ログ**（発注書のどの記述をどう読んだか。特に曖昧だと感じた箇所と、そこで採った解釈を全て書く）:
