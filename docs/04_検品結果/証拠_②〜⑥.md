status: draft
# 証拠ファイル — 走行②〜⑥（CC記入用）

**この雛形を埋めるのはCC（実装したセッションでよい。新規セッション不要）。** CLAUDE.md 12章の運用に従うこと。

- 各セクション冒頭の「検品前に実行するコマンド」を実行し、出力を**そのまま**貼る。
- 続く各行の「証拠」欄に、対応するコマンドの該当出力・DB確認結果・画面で確認した文字列や挙動を**生のまま**書く。要約・言い換えをしない。
- **「YES」「PASS」「合格」「正しく動作した」「問題ない」等の判定語は一切書かない。** 判定はCCの外（Claudeとのチャット）で行う。判定語が書かれた報告は無効として再提出を求める。
- 確認できなかった行は空欄のまま提出する。体裁を整えるための推測・決め打ちの記入をしない（空欄＝未確認としてNO扱いになる）。
- 埋め終えたら片倉がこのファイルをそのままチャットへアップロードする。

---

## 走行② — 書籍CRUD
対象検品表: `docs/03_検品表/02_書籍CRUD.md`

### 検品前に実行するコマンド（出力をまとめてここに貼る）
```
（ここに実行結果を貼る）
```
（参考: 実行するコマンド）
```bash
git branch --show-current
git log --oneline -5
sail artisan route:list --path=books
sail bin pint --test
```

### 行ごとの証拠

#### A. ルーティング
**A-1** — `route:list` に `books.index` `books.create` `books.store` `books.show` `books.edit` `books.update` `books.destroy` `books.restore` の8本がすべて存在する
確認方法: `sail artisan route:list --path=books`
証拠:
```

```
**A-2** — `/` が `BookController@index` に束ねられ、名前が付いていない
確認方法: `route:list` に `/` の行があり `Name` 列が空
証拠:
```

```
**A-3** — `/books/create` の定義が `/books/{book}` より前にある（`route:list`で`books.create`が404にならない）
確認方法: `curl -I http://localhost/books/create`（未ログイン時は302で`/login`へ）
証拠:
```

```
**A-4** — `books.show` と `books.restore` に `withTrashed()` が付いている
確認方法: ソース確認（`grep withTrashed routes/web.php`）
証拠:
```

```
**A-5** — `books.edit` `books.update` `books.destroy` に `withTrashed()` が**付いていない**
確認方法: 同上
証拠:
```

```

#### B. コントローラー・認証
**B-1** — `books.index` `books.show` は未ログインで200が返る
確認方法: `curl -I`
証拠:
```

```
**B-2** — `books.create` `books.store` `books.edit` `books.update` `books.destroy` `books.restore` は未ログインで`/login`へ302リダイレクトされる
確認方法: `curl -I`
証拠:
```

```
**B-3** — 一覧・詳細画面でクエリが重複発行されていない（N+1がない）
確認方法: `DB::listen`またはdebugbarでクエリ数確認。一覧はページあたり定数回
証拠:
```

```

#### C. 登録（store）
**C-1** — 全項目を正しく入力して登録すると `books.show` へ遷移し「書籍を登録しました」が表示される
確認方法: 実機
証拠:
```

```
**C-2** — title/author/isbn/published_date 未入力、isbn不正（13桁でない）、isbn重複、genre未選択の各ケースで`back()`+`$errors`によりエラー文言が表示される
確認方法: 実機（本書§4のメッセージと一致すること）
証拠:
```

```
**C-3** — 選択したジャンルのみが `book_genre` に sync される
確認方法: DB確認
証拠:
```

```
**C-4** — 登録は `user_id: Auth::id()` で保存される
確認方法: DB確認
証拠:
```

```

#### D. 編集（edit/update）
**D-1** — 登録者本人はedit/updateを実行でき、更新後 `books.show` へ「書籍を更新しました」で遷移する
確認方法: 実機
証拠:
```

```
**D-2** — 登録者本人以外がedit/updateにアクセスすると403
確認方法: 実機（別ユーザーでログイン）
証拠:
```

```
**D-3** — ISBN一意性チェックは自身のレコードを除外する（同じISBNのまま更新してもエラーにならない）
確認方法: 実機
証拠:
```

```
**D-4** — 更新時もジャンルの選択が `sync()` で反映される
確認方法: DB確認
証拠:
```

```

#### E. 削除（destroy）と論理削除
**E-1** — 登録者本人が削除すると `books.index` へ「書籍を削除しました」で遷移する
確認方法: 実機
証拠:
```

```
**E-2** — 登録者本人以外が削除しようとすると403
確認方法: 実機
証拠:
```

```
**E-3** — 削除後、`books` テーブルにレコードが物理的に残っており `deleted_at` が入っている（物理削除されていない）
確認方法: `SELECT * FROM books WHERE id=? ` を`sail exec mysql`で直接確認
証拠:
```

```
**E-4** — 削除後、`reviews` / `favorites` / `book_genre` のレコードが保持されている（消えていない）
確認方法: DB確認
証拠:
```

```
**E-5** — 削除済み書籍が一覧・ランキングから除外される
確認方法: 実機
証拠:
```

```
**E-6** — 削除済み書籍のISBNで新規登録しようとすると一意性エラーになる（`withTrashed()`除外していない）
確認方法: 実機
証拠:
```

```

#### F. 削除済み書籍詳細ページ
**F-1** — 削除済み書籍の詳細URLに直接アクセスしても404にならず表示される
確認方法: 実機
証拠:
```

```
**F-2** — ページ上部に「この本は削除されました」バナーが表示される
確認方法: 実機
証拠:
```

```
**F-3** — レビュー・評価一覧がそのまま表示される（本人以外の分も含む）
確認方法: 実機
証拠:
```

```
**F-4** — 編集・削除ボタンが非表示になっている
確認方法: 実機
証拠:
```

```
**F-5** — お気に入りボタンが非表示になっている
確認方法: 実機
証拠:
```

```
**F-6** — 新規レビュー投稿フォームが非表示で「削除済みの書籍にはレビューを投稿できません」に差し替わっている
確認方法: 実機
証拠:
```

```
**F-7** — 登録者本人にのみ「復元する」ボタンが表示される（別ユーザーでは出ない）
確認方法: 実機（2ユーザーで確認）
証拠:
```

```
**F-8** — §8-1に明記した4点以外、`books/show.blade.php` の既存マークアップ・クラス名・レイアウトが変更されていない
確認方法: モックリポジトリ(basic)を一時cloneし、変更箇所を目視で4点に限定できるか確認
証拠:
```

```

#### G. 復元（restore）
**G-1** — 登録者本人が「復元する」を押すと通常状態に戻り、一覧・ランキングに再表示される
確認方法: 実機
証拠:
```

```
**G-2** — 復元後、`deleted_at` が NULL に戻っている
確認方法: DB確認
証拠:
```

```
**G-3** — 登録者本人以外が `PATCH /books/{book}/restore` に直接アクセスすると403になる
確認方法: 実機（別ユーザーで直URL）
証拠:
```

```
**G-4** — 未削除の書籍に対して `restore` を実行しても403になる（`BookPolicy::restore`が`trashed()`を条件に含めている）
確認方法: 実機
証拠:
```

```

#### H. コード品質・スコープ厳守
**H-1** — `sail bin pint --test` が「No fixable issues were found」
確認方法: コマンド出力
証拠:
```

```
**H-2** — バリデーションが `StoreBookRequest` / `UpdateBookRequest` に分離されている（コントローラー内に`validate()`が書かれていない）
確認方法: ソース確認
証拠:
```

```
**H-3** — 認可が `BookPolicy` に実装され、コントローラーで `$this->authorize()` が呼ばれている（コントローラー内で `if ($book->user_id !== Auth::id())` 等の手書き判定をしていない）
確認方法: ソース確認
証拠:
```

```
**H-4** — 発注書に明記されていない独自ロジック（例: 独自の削除確認ステップ、発注書にないバリデーション項目、発注書にない画面）が追加されていない
確認方法: ソース確認・実機
証拠:
```

```
**H-5** — migration が変更されていない（`git diff main -- database/migrations/` が空）
確認方法: `git diff`
証拠:
```

```
**H-6** — `QUESTIONS.md` に走行②由来の未解消行がない、または全て記録されている
確認方法: `QUESTIONS.md` 確認
証拠:
```

```

## 走行③ — レビューお気に入りいいね
対象検品表: `docs/03_検品表/03_レビューお気に入りいいね.md`

### 検品前に実行するコマンド（出力をまとめてここに貼る）
```
（ここに実行結果を貼る）
```
（参考: 実行するコマンド）
```bash
git branch --show-current
git log --oneline -5
sail artisan route:list --path=reviews
sail artisan route:list --path=favorites
sail bin pint --test
```

### 行ごとの証拠

#### A. ルーティング
**A-1** — `reviews.store` `reviews.edit` `reviews.update` `reviews.destroy` `reviews.like` の5本が存在する
確認方法: `route:list --path=reviews`
証拠:
```

```
**A-2** — `favorites.index` `favorites.toggle` の2本が存在する
確認方法: `route:list --path=favorites`
証拠:
```

```
**A-3** — 専用の `LikeController` が作られておらず、いいねが `ReviewController@like` に統合されている
確認方法: ソース確認
証拠:
```

```
**A-4** — `FavoriteController` が独立して存在する（`ReviewController`に統合されていない）
確認方法: ソース確認
証拠:
```

```
**A-5** — 全7ルートが `auth` ミドルウェア配下にある
確認方法: ソース確認
証拠:
```

```

#### B. レビュー投稿・編集・削除
**B-1** — `rating`必須・`comment`任意で投稿でき、`books.show`へ遷移し「レビューを投稿しました」が表示される
確認方法: 実機
証拠:
```

```
**B-2** — `rating`未入力・範囲外（0や6）でエラーが表示される。エラー文言が本書§4と一致する
確認方法: 実機
証拠:
```

```
**B-3** — 投稿者本人は編集・削除ができ、それ以外は403になる
確認方法: 実機（2ユーザーで確認）
証拠:
```

```
**B-4** — レビュー編集画面で `$review->book->title` が正しく表示される（削除済み書籍のレビューでも落ちない）
確認方法: 実機（削除済み書籍のレビューを編集）
証拠:
```

```
**B-5** — レビュー削除時、紐づく `review_likes` が連動して削除される（cascade）
確認方法: DB確認（削除前後の`review_likes`件数）
証拠:
```

```
**B-6** — 削除後のリダイレクト先が正しく解決される（`$review->book`を削除前に取得している）
確認方法: ソース確認・実機
証拠:
```

```

#### C. いいねトグル
**C-1** — 「いいね」ボタンでON/OFFが `review_likes` に正しく反映される
確認方法: 実機・DB確認
証拠:
```

```
**C-2** — いいね操作でflashメッセージが表示されない
確認方法: 実機
証拠:
```

```
**C-3** — 参照先の書籍が削除済みのレビューでも、いいね操作が引き続き可能である
確認方法: 実機（削除済み書籍のレビューにいいね）
証拠:
```

```
**C-4** — 未ログインでいいねを押すと `/login` へリダイレクトされる
確認方法: 実機
証拠:
```

```

#### D. お気に入りトグル・一覧
**D-1** — 未登録の書籍でボタンを押すと `favorites` にレコードが作成される
確認方法: 実機・DB確認
証拠:
```

```
**D-2** — 登録済みの書籍で再度押すと `favorites` からレコードが削除される
確認方法: 実機・DB確認
証拠:
```

```
**D-3** — お気に入り操作でflashメッセージが表示されない
確認方法: 実機
証拠:
```

```
**D-4** — 未ログインで `/favorites` にアクセスすると `/login` へリダイレクトされる
確認方法: 実機
証拠:
```

```
**D-5** — お気に入り一覧に自分が登録した書籍のみが表示される（他ユーザーの分は出ない）
確認方法: 実機（2ユーザーで確認）
証拠:
```

```
**D-6** — お気に入り一覧の並び順が `books.created_at` 降順である
確認方法: 実機
証拠:
```

```

#### E. 削除済み書籍との関係
**E-1** — 論理削除済みの書籍がお気に入り一覧から表示されない
確認方法: 実機
証拠:
```

```
**E-2** — 書籍削除後も `favorites` レコードが保持される（消えない）
確認方法: DB確認
証拠:
```

```
**E-3** — 書籍を復元するとお気に入り一覧に再び現れる
確認方法: 実機
証拠:
```

```

#### F. コード品質・スコープ厳守
**F-1** — `sail bin pint --test` が「No fixable issues were found」
確認方法: コマンド出力
証拠:
```

```
**F-2** — バリデーションが `StoreReviewRequest` / `UpdateReviewRequest` に分離されている
確認方法: ソース確認
証拠:
```

```
**F-3** — 認可が `ReviewPolicy` に実装され、コントローラーで `$this->authorize()` が呼ばれている
確認方法: ソース確認
証拠:
```

```
**F-4** — `ReviewPolicy` の判定に書籍の削除状態（`$book->trashed()`）が**含まれていない**
確認方法: ソース確認（`BookPolicy`との混同がないか）
証拠:
```

```
**F-5** — 発注書に明記されていない独自ロジックが追加されていない
確認方法: ソース確認・実機
証拠:
```

```
**F-6** — migration が変更されていない
確認方法: `git diff main -- database/migrations/`
証拠:
```

```
**F-7** — `QUESTIONS.md` に走行③由来の未解消行がない、または全て記録されている
確認方法: `QUESTIONS.md` 確認
証拠:
```

```

## 走行④ — ジャンルランキング
対象検品表: `docs/03_検品表/04_ジャンルランキング.md`

### 検品前に実行するコマンド（出力をまとめてここに貼る）
```
（ここに実行結果を貼る）
```
（参考: 実行するコマンド）
```bash
git branch --show-current
git log --oneline -5
sail artisan route:list --path=genres
sail artisan route:list --path=ranking
sail bin pint --test
```

### 行ごとの証拠

#### A. ルーティング
**A-1** — `genres.index/create/store/show/edit/update/destroy`の7本が存在する
確認方法: `route:list --path=genres`
証拠:
```

```
**A-2** — `ranking.index`が存在し、`auth`ミドルウェアが付いていない
確認方法: `route:list --path=ranking`
証拠:
```

```
**A-3** — `App\Policies\GenrePolicy`が存在しない
確認方法: ファイル確認（`ls app/Policies/`）
証拠:
```

```

#### B. ジャンル一覧・詳細
**B-1** — 一覧に各ジャンルの書籍数が表示される（`books_count`）
確認方法: 実機
証拠:
```

```
**B-2** — 書籍数は削除済み書籍を含まない生存書籍のみでカウントされている
確認方法: 実機（削除済み書籍しか紐付かないジャンルで0冊表示を確認）
証拠:
```

```
**B-3** — ジャンル詳細に紐づく書籍が10件ページネーションで表示される
確認方法: 実機
証拠:
```

```
**B-4** — 詳細画面の各書籍タグで、表示中ジャンルと一致するものだけ色が変わる
確認方法: 実機
証拠:
```

```

#### C. 登録・編集
**C-1** — `name`未入力でエラーが表示される
確認方法: 実機
証拠:
```

```
**C-2** — `name`重複でエラーが表示される（「このジャンル名は既に登録されています」）
確認方法: 実機
証拠:
```

```
**C-3** — 編集は所有者概念がなく、ログイン済みであれば誰でも実行できる（作成者でなくても編集できる）
確認方法: 実機（別ユーザーで編集）
証拠:
```

```
**C-4** — 編集時の一意性チェックが自身のレコードを除外する
確認方法: 実機
証拠:
```

```

#### D. 削除制限（二重防御）
**D-1** — 紐づく書籍が1件でも存在する場合、error flash「このジャンルに紐づく書籍が存在するため削除できません」とともに削除が失敗し、レコードが残る
確認方法: 実機
証拠:
```

```
**D-2** — 紐づく書籍が存在しない場合は削除でき、「ジャンルを削除しました」が表示される
確認方法: 実機
証拠:
```

```
**D-3** — アプリ側チェックが `withTrashed()` 付きである（削除済み書籍にのみ紐づくジャンルでも削除がブロックされ、500エラーにならない）
確認方法: 実機（該当ジャンルを作って確認）
証拠:
```

```
**D-4** — 二重防御 — アプリ側チェックを迂回（`GenreController@destroy`を経由しないtinker等での直接delete）してもDB制約`restrictOnDelete()`で削除が防止される
確認方法: `sail artisan tinker`で紐付きのあるジャンルを直接`->delete()`し例外が出ることを確認
証拠:
```

```

#### E. ランキング
**E-1** — 平均評価（`reviews_avg_rating`）の降順でTOP10が表示される
確認方法: 実機
証拠:
```

```
**E-2** — レビューが1件もない書籍はランキング対象外である
確認方法: 実機
証拠:
```

```
**E-3** — 論理削除済みの書籍がランキングの集計・表示対象から除外される
確認方法: 実機（書籍を削除して確認）
証拠:
```

```
**E-4** — 削除した書籍を復元するとランキングに再び現れる
確認方法: 実機
証拠:
```

```
**E-5** — 未ログインでも `/ranking` を閲覧できる
確認方法: 実機
証拠:
```

```
**E-6** — 変数名が `$rankedBooks` である（`$books`ではない）
確認方法: ソース確認
証拠:
```

```
**E-7** — 平均評価が本文で小数第2位、右バッジで小数第1位の両方で表示される
確認方法: 実機
証拠:
```

```

#### F. コード品質・スコープ厳守
**F-1** — `sail bin pint --test` が「No fixable issues were found」
確認方法: コマンド出力
証拠:
```

```
**F-2** — バリデーションが `StoreGenreRequest` / `UpdateGenreRequest` に分離されている
確認方法: ソース確認
証拠:
```

```
**F-3** — ランキングのクエリが `withAvg`/`withCount`/`whereHas`で構成され、`with('reviews')`で全件ロードしていない
確認方法: ソース確認
証拠:
```

```
**F-4** — 発注書に明記されていない独自ロジックが追加されていない
確認方法: ソース確認・実機
証拠:
```

```
**F-5** — migration が変更されていない
確認方法: `git diff main -- database/migrations/`
証拠:
```

```
**F-6** — `QUESTIONS.md` に走行④由来の未解消行がない、または全て記録されている
確認方法: `QUESTIONS.md` 確認
証拠:
```

```

## 走行⑤ — 公開APIシーディング
対象検品表: `docs/03_検品表/05_公開APIシーディング.md`

### 検品前に実行するコマンド（出力をまとめてここに貼る）
```
（ここに実行結果を貼る）
```
（参考: 実行するコマンド）
```bash
git branch --show-current
git log --oneline -5
sail artisan migrate:fresh --seed
sail exec mysql mysql -usail -ppassword laravel -e "SELECT COUNT(*) FROM users;"
sail exec mysql mysql -usail -ppassword laravel -e "SELECT COUNT(*) FROM genres;"
sail exec mysql mysql -usail -ppassword laravel -e "SELECT COUNT(*) FROM books;"
sail exec mysql mysql -usail -ppassword laravel -e "SELECT COUNT(*) FROM reviews;"
sail exec mysql mysql -usail -ppassword laravel -e "SELECT COUNT(*) FROM favorites;"
sail exec mysql mysql -usail -ppassword laravel -e "SELECT COUNT(*) FROM review_likes;"
sail artisan route:list --path=api
sail bin pint --test
```

### 行ごとの証拠

#### A. シーディング（採点直結）
**A-1** — `sail artisan db:seed` がエラーなく完了する
確認方法: コマンド出力
証拠:
```

```
**A-2** — users 5件・genres 10件・books 11件・reviews 32件が投入されている
確認方法: COUNT結果
証拠:
```

```
**A-3** — `db:seed` を2回実行しても重複が発生しない（`firstOrCreate`/`syncWithoutDetaching`が使われている）
確認方法: 2回実行してCOUNTが変わらないことを確認
証拠:
```

```
**A-4** — GenreSeederの投入順・書籍データ・ユーザーデータが要件シート シート9の記載と1文字違わず一致する
確認方法: 発注書§8と実データを突合
証拠:
```

```
**A-5** — `id=3`の書籍が「リーダブルコード」、`genre_id=3`が「技術書」になっている
確認方法: DB確認
証拠:
```

```
**A-6** — 各書籍に`description`と`image_url`（`https://placehold.co/200x300/e2e8f0/475569?text={番号}`形式）が設定されている
確認方法: DB確認
証拠:
```

```
**A-7** — 各レビューのいいねが自分のレビューを除いて0〜3人分投入されている
確認方法: DB確認（サンプル抽出）
証拠:
```

```

#### B. AP01 書籍一覧
**B-1** — パラメータなしで200が返り、`per_page=10`のページネーションが既定で適用される
確認方法: `curl`
証拠:
```

```
**B-2** — `keyword`/`genre_id`/`page`/`per_page`のいずれかが不正な場合422が返る（エラー文言が発注書§3と一致）
確認方法: `curl`
証拠:
```

```
**B-3** — レスポンスに`description`が含まれない
確認方法: `curl`のJSON確認
証拠:
```

```
**B-4** — `genres`/`average_rating`/`reviews_count`が各書籍に含まれる
確認方法: 同上
証拠:
```

```
**B-5** — 該当0件でも`data: []`で200が返る
確認方法: `curl`
証拠:
```

```
**B-6** — ページ送りで検索条件が維持される（`withQueryString()`）
確認方法: `curl`で`links`確認
証拠:
```

```

#### C. AP02 書籍詳細
**C-1** — 存在するIDで200が返り、`reviews`に投稿者名・評価・コメント・投稿日時が含まれる
確認方法: `curl`
証拠:
```

```
**C-2** — `comment`がnullのレビューを含んでも正しく返る（keyが落ちない）
確認方法: `curl`
証拠:
```

```
**C-3** — `created_at`がISO8601・`+09:00`オフセット付きで返る
確認方法: `curl`
証拠:
```

```
**C-4** — 存在しないIDで404、`{"message": "指定された書籍が見つかりません。"}`が返る（`errors`キーなし）
確認方法: `curl`
証拠:
```

```
**C-5** — 論理削除済みの書籍IDを指定すると404が返る
確認方法: `curl`（書籍を削除してから確認）
証拠:
```

```

#### D. AP03 書籍登録
**D-1** — 正しいデータで201が返り、レスポンスに`reviews`キーが含まれない
確認方法: `curl`
証拠:
```

```
**D-2** — レスポンスに`user_id`が含まれない
確認方法: `curl`
証拠:
```

```
**D-3** — バリデーション違反で422、`{"message": "入力内容に誤りがあります。", "errors": {...}}`形式で返る
確認方法: `curl`
証拠:
```

```
**D-4** — `genres`配列で指定したジャンルのみが`book_genre`にsyncされる
確認方法: DB確認
証拠:
```

```

#### E. AP04 書籍更新
**E-1** — 正しいデータで200が返る
確認方法: `curl`
証拠:
```

```
**E-2** — ISBN一意性チェックが自身のレコードを除外する（同じISBNのまま更新してもエラーにならない）
確認方法: `curl`
証拠:
```

```
**E-3** — 存在しないIDで404、バリデーション違反で422が返る
確認方法: `curl`
証拠:
```

```

#### F. AP05 書籍削除
**F-1** — 204（本文なし）が返る
確認方法: `curl -i`
証拠:
```

```
**F-2** — 対象書籍が物理削除ではなく論理削除される（`deleted_at`が入る）
確認方法: DB確認
証拠:
```

```
**F-3** — 存在しないIDで404が返る
確認方法: `curl`
証拠:
```

```
**F-4** — 削除後、`reviews`/`favorites`/`book_genre`のレコードが保持される
確認方法: DB確認
証拠:
```

```

#### G. コード品質・スコープ厳守
**G-1** — `sail bin pint --test` が「No fixable issues were found」
確認方法: コマンド出力
証拠:
```

```
**G-2** — バリデーションが`Api\V1`名前空間のFormRequestに分離されている
確認方法: ソース確認
証拠:
```

```
**G-3** — API側にPolicy（`$this->authorize()`）が1箇所も呼ばれていない
確認方法: ソース確認
証拠:
```

```
**G-4** — `Api\V1\BookController`と`BookController`（Web版）が別クラスとして両方存在する
確認方法: ソース確認
証拠:
```

```
**G-5** — 発注書に明記されていない独自ロジックが追加されていない
確認方法: ソース確認
証拠:
```

```
**G-6** — migration が変更されていない（`personal_access_tokens`が作られていない）
確認方法: `git diff main -- database/migrations/`
証拠:
```

```
**G-7** — `QUESTIONS.md` に走行⑤由来の未解消行がない、または全て記録されている
確認方法: `QUESTIONS.md` 確認
証拠:
```

```

## 走行⑥ — 基本テスト
対象検品表: `docs/03_検品表/06_基本テスト.md`

### 検品前に実行するコマンド（出力をまとめてここに貼る）
```
（ここに実行結果を貼る）
```
（参考: 実行するコマンド）
```bash
git branch --show-current
git log --oneline -5
sail artisan test
sail artisan test --coverage
sail bin pint --test
```

### 行ごとの証拠

#### A. 実行結果
**A-1** — `sail artisan test` が全件PASSする（FAILが0件）
確認方法: コマンド出力
証拠:
```

```
**A-2** — `sail artisan test --coverage` の基本機能カバレッジが60%を超える
確認方法: コマンド出力
証拠:
```

```
**A-3** — テスト実行のたびに結果が変わらない（フレーキーでない）。同一コマンドを2回実行して結果が一致する
確認方法: 2回実行して比較
証拠:
```

```

#### B. テストファイルの存在と観点網羅
**B-1** — `tests/Feature/ScreenAccessTest.php` が存在し、F-A1〜F-A4の4観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-2** — `tests/Feature/AuthTest.php` が存在し、F-A5〜F-A9の5観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-3** — `tests/Unit/BookTest.php` が存在し、U-B1〜U-B5の5観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-4** — `tests/Feature/BookCrudTest.php` が存在し、F-B1〜F-B13の13観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-5** — `tests/Unit/ReviewTest.php` が存在し、U-R1〜U-R4の4観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-6** — `tests/Feature/ReviewTest.php` が存在し、F-R1〜F-R5の5観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-7** — `tests/Feature/ReviewLikeTest.php` が存在し、F-L1〜F-L2の2観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-8** — `tests/Unit/FavoriteTest.php` が存在し、U-F1に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-9** — `tests/Feature/FavoriteTest.php` が存在し、F-F1〜F-F5の5観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-10** — `tests/Unit/GenreTest.php` が存在し、U-G1に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-11** — `tests/Feature/GenreTest.php` が存在し、F-G1〜F-G6の6観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-12** — `tests/Feature/RankingTest.php` が存在し、F-K1〜F-K4の4観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-13** — `tests/Feature/Api/BookApiTest.php` が存在し、F-P1〜F-P10の10観点に対応するテストメソッドがある
確認方法: ソース確認
証拠:
```

```
**B-14** — `tests/Feature/SeederTest.php` が存在し、F-P11〜F-P12に対応するテストメソッドがある（推奨扱いだが、存在しない場合はNOとして記録し、片倉の裁定を仰ぐ）
確認方法: ソース確認
証拠:
```

```

#### C. テストの質（形骸化していないか）
**C-1** — 認可系テスト（F-B4/F-B5/F-B11/F-R2/F-R3/F-G3）で「本人」と「それ以外」が明確に別の`User`インスタンスとして扱われている（同一ユーザーで403判定を回避していない）
確認方法: ソース確認
証拠:
```

```
**C-2** — SoftDeletes系テスト（F-B6〜F-B13/F-K3）が`assertDatabaseHas`等でDBレベルの保持を確認しており、画面表示の確認だけで済ませていない
確認方法: ソース確認
証拠:
```

```
**C-3** — 各テストメソッドが該当観点に無関係なアサーションだけで「グリーン」になっていない（例: レスポンスコードだけ見てレスポンスボディの構造を見ていない、等の手抜きがない）
確認方法: ソース確認（抜き取りで3件）
証拠:
```

```
**C-4** — テストで見つかった実装不具合が、この走行内で無断修正されていない（`QUESTIONS.md`に記録されているか、実装側コミットに走行⑥のコミットが混ざっていないか）
確認方法: `git log`で走行②〜⑤対象ファイルへの変更がないか確認
証拠:
```

```

#### D. コード品質・スコープ厳守
**D-1** — `sail bin pint --test` が「No fixable issues were found」
確認方法: コマンド出力
証拠:
```

```
**D-2** — migration・実装コード（Controller/Model/Policy/FormRequest）が変更されていない（`git diff main --stat`でtests/配下以外に差分がない）
確認方法: `git diff`
証拠:
```

```
**D-3** — `QUESTIONS.md` に走行⑥由来の未解消行がない、または全て記録されている
確認方法: `QUESTIONS.md` 確認
証拠:
```

```