status: submitted
# 証拠ファイル — 走行②〜⑥（CC記入）

記入方針（CLAUDE.md 12章 / 雛形冒頭）: 各証拠欄には実行したコマンドとその**生の標準出力のみ**を貼る。要約・言い換え・判定語（YES/PASS/合格/正しく動作した/問題ない等）はCCとして書かない。証拠中に現れる `✓` `PASS` `DONE` `RUNNING` はツール（Pest/PHPUnit・artisan）の標準出力そのものである。判定はチャット側で行う。

環境: アプリURL `http://localhost:8022`（`APP_PORT=8022`）。以下 `$B=http://localhost:8022`。API/curl はこのURLに対して実行。テストは `phpunit.xml` の `DB_DATABASE=testing`（別DB）に対して実行。
基準コミット: 走行①〜⑥は未マージのfeatureブランチにスタック中。migration/モデルの無変更確認は走行①完了時点のコミット `6a346be` を基準に `git diff` を取得（`main`(c2eaa2c) は走行①より前のため基準に不適）。

---

## 走行② — 書籍CRUD
対象検品表: `docs/03_検品表/02_書籍CRUD.md`

### 検品前に実行するコマンド（出力）
```
$ git branch --show-current
feature/06-basic-test

$ git log --oneline -8
f979e21 test: 基本機能のUnit/Featureテストを実装（走行⑥）
c4fb811 feat: 公開API(v1) 5本とシーディング6本を実装
9bd1ee7 feat: ジャンルCRUD・評価ランキングを実装
514e4d3 feat: レビュー・お気に入り・いいねを実装
63ccbd8 feat: 書籍CRUD（一覧/詳細/登録/編集/論理削除/復元）を実装
6a346be chore: 走行①のBladeモック・docs・CLAUDE.md等を確定
e07e926 fix: ログイン認証をFortify標準へ戻しauthenticateUsingを除去
c40b3b7 feat: 土台構築とFortifyによるセッション認証を実装

$ sail artisan route:list --path=books
  GET|HEAD   api/v1/books ..... books.index › Api\V1\BookController@index
  POST       api/v1/books ..... books.store › Api\V1\BookController@store
  GET|HEAD   api/v1/books/{book} books.show › Api\V1\BookController@show
  PUT|PATCH  api/v1/books/{book} books.update › Api\V1\BookController@update
  DELETE     api/v1/books/{book} books.destroy › Api\V1\BookController@destroy
  GET|HEAD   books ................... books.index › BookController@index
  POST       books ................... books.store › BookController@store
  GET|HEAD   books/create .......... books.create › BookController@create
  GET|HEAD   books/{book} .............. books.show › BookController@show
  PUT        books/{book} .......... books.update › BookController@update
  DELETE     books/{book} ........ books.destroy › BookController@destroy
  GET|HEAD   books/{book}/edit ......... books.edit › BookController@edit
  POST       books/{book}/favorites favorites.toggle › FavoriteController@toggle
  PATCH      books/{book}/restore books.restore › BookController@restore
  POST       books/{book}/reviews reviews.store › ReviewController@store
                                                            Showing [15] routes

$ sail bin pint --test
  ──────────────────────────────────────────────────────────────────── Laravel
    PASS   ......................................................... 107 files
```

### 行ごとの証拠

#### A. ルーティング
**A-1**
証拠:
```
$ sail artisan route:list --path=books   （上記の8本）
  GET|HEAD   books ................... books.index › BookController@index
  POST       books ................... books.store › BookController@store
  GET|HEAD   books/create .......... books.create › BookController@create
  GET|HEAD   books/{book} .............. books.show › BookController@show
  PUT        books/{book} .......... books.update › BookController@update
  DELETE     books/{book} ........ books.destroy › BookController@destroy
  GET|HEAD   books/{book}/edit ......... books.edit › BookController@edit
  PATCH      books/{book}/restore books.restore › BookController@restore
```
**A-2**
証拠:
```
$ sail artisan route:list | grep -E "GET\|HEAD +/ "
  GET|HEAD        / ..................................... BookController@index
（"books.index ›" のような name 列が無く BookController@index のみ。名前付きルートは "name ›" 表記になる）
```
**A-3**
証拠:
```
$ curl -s -i $B/books/create
HTTP/1.1 302 Found
Location: http://localhost:8022/login
```
**A-4**
証拠:
```
$ grep -n withTrashed routes/web.php
32:Route::get('/books/{book}', [BookController::class, 'show'])->name('books.show')->withTrashed();
40:        ->name('books.restore')->withTrashed();
```
**A-5**
証拠:
```
$ grep -n withTrashed routes/web.php
32:Route::get('/books/{book}', [BookController::class, 'show'])->name('books.show')->withTrashed();
40:        ->name('books.restore')->withTrashed();
（一致は show(32)・restore(40) の2行のみ。books.edit / books.update / books.destroy の行は grep に一致しない）
```

#### B. コントローラー・認証
**B-1**
証拠:
```
$ curl -s -i $B/books    | head -1  ->  HTTP/1.1 200 OK
$ curl -s -i $B/books/3  | head -1  ->  HTTP/1.1 200 OK
```
**B-2**
証拠:
```
$ curl -s -i $B/books/create ; $B/books/3/edit
/books/create : HTTP/1.1 302 Found   Location: http://localhost:8022/login
/books/3/edit : HTTP/1.1 302 Found   Location: http://localhost:8022/login

$ sail artisan test --filter=ScreenAccessTest
  ✓ guest redirected from auth pages
$ sail artisan test --filter=AuthTest
  ✓ logout requires login again
（store/update/destroy/restore は routes/web.php で Route::middleware('auth')->group 内に定義。上記テストで未認証時の /login リダイレクトを実行）
```
**B-3**
証拠:
```
$ grep -nE "with\(|withAvg|load\(|latest\(|paginate" app/Http/Controllers/BookController.php
19:        $books = Book::with('genres')
20:            ->withAvg('reviews', 'rating')
21:            ->latest()
22:            ->paginate(10);
57:        $book->load([
59:            'reviews' => fn ($q) => $q->latest(),
（index は with('genres')+withAvg、show は load でまとめて解決）
```

#### C. 登録（store）
**C-1**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ store book success
$ grep -n "書籍を登録しました" app/Http/Controllers/BookController.php
49:        return redirect()->route('books.show', $book)->with('success', '書籍を登録しました');
（テスト本体: assertRedirect(route('books.show',$book)) / assertSessionHas('success','書籍を登録しました') / assertDatabaseHas('books',[...]))
```
**C-2**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ store validation errors
$ grep -nE "required|regex|unique|max:255|min:1" app/Http/Requests/StoreBookRequest.php  （抜粋・rules/messages）
  'title' => ['required', 'string', 'max:255'],
  'isbn'  => ['required', 'string', 'regex:/^[0-9]{13}$/', 'unique:books,isbn'],
  'genres'=> ['required', 'array', 'min:1'],
  'title.required' => 'タイトルを入力してください',
  'isbn.regex'    => 'ISBNは13桁の数字で入力してください',
  'isbn.unique'   => 'このISBNは既に登録されています',
  'genres.required'=> 'ジャンルを1つ以上選択してください',
（テスト本体: 空POST -> assertSessionHasErrors(['title','author','isbn','published_date','genres']) / isbn='123' -> errors('isbn') / 重複ISBN -> errors('isbn')）
```
**C-3**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ genre sync on store and update
$ sail artisan tinker --execute="echo App\Models\Book::find(2)->genres->pluck('name')->implode(',');"
ビジネス,自己啓発
（book_id=2「人を動かす」の book_genre は指定2件のみ）
```
**C-4**
証拠:
```
$ grep -n "Auth::id()" app/Http/Controllers/BookController.php
43:            $book = Book::create($request->validated() + ['user_id' => Auth::id()]);
$ sail artisan test --filter=BookCrudTest
  ✓ store book success
（テスト本体: assertDatabaseHas('books',['title'=>'テスト書籍','user_id'=>$user->id])）
```

#### D. 編集（edit/update）
**D-1**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ genre sync on store and update
$ grep -n "書籍を更新しました" app/Http/Controllers/BookController.php
91:        return redirect()->route('books.show', $book)->with('success', '書籍を更新しました');
```
**D-2**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ edit authorization
（テスト本体: owner get(books.edit)->assertOk() / other get(books.edit)->assertForbidden() )
```
**D-3**
証拠:
```
$ grep -n "ignore" app/Http/Requests/UpdateBookRequest.php
32:                Rule::unique('books', 'isbn')->ignore($this->route('book')),
$ curl -s -i -X PUT $B/api/v1/books/1 -d '{... isbn:"9784101010014"(book1自身のISBN) ...}'
HTTP/1.1 200 OK
{"data":{"id":1,"title":"吾輩は猫である(改)","isbn":"9784101010014",...}}
```
**D-4**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ genre sync on store and update
（テスト本体: 編集で別ジャンルのみ指定 -> assertEqualsCanonicalizing([$genre2], $book->fresh()->genres->pluck('id'))）
```

#### E. 削除（destroy）と論理削除
**E-1**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ delete authorization
$ grep -n "書籍を削除しました" app/Http/Controllers/BookController.php
103:        return redirect()->route('books.index')->with('success', '書籍を削除しました');
（テスト本体: owner delete(books.destroy)->assertRedirect(route('books.index')) / deleted_at がセット）
```
**E-2**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ delete authorization
（テスト本体: other delete(books.destroy)->assertForbidden() / assertNull($book->fresh()->deleted_at)）
```
**E-3**
証拠:
```
$ curl -s -i -X DELETE $B/api/v1/books/11   ->  HTTP/1.1 204 No Content
$ sail artisan tinker --execute="\$b=Book::withTrashed()->find(11); echo 'deleted_at='.\$b->deleted_at;"
deleted_at=2026-09-02 12:07:36
（id=11 の行は存在し deleted_at がセットされる＝物理削除されていない）
```
**E-4**
証拠:
```
$ sail artisan tinker --execute="echo 'reviews='.Review::where('book_id',11)->count().' book_genre='.DB::table('book_genre')->where('book_id',11)->count();"
reviews=3 book_genre=2
$ sail artisan test --filter=BookCrudTest
  ✓ reviews kept after book soft delete
（削除済み book_id=11 の reviews・book_genre が残存）
```
**E-5**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ deleted book excluded from listings
（テスト本体: get('/books')->assertDontSee($title) / get('/ranking')->assertDontSee($title) / getJson('/api/v1/books')->assertJsonMissing(['id'=>$id])）
```
**E-6**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ deleted isbn uniqueness
（テスト本体: 削除済みbookのISBNで post('/books') -> assertSessionHasErrors('isbn')）
```

#### F. 削除済み書籍詳細ページ
**F-1**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ deleted book show page
（テスト本体: get(route('books.show',$deletedBook))->assertOk()）
```
**F-2**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ deleted book show page
$ git diff 6a346be -- resources/views/books/show.blade.php  （バナー追記箇所）
+            @if ($book->trashed())
+                <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
+                    この本は削除されました
+                </div>
+            @endif
（テスト本体: ->assertSee('この本は削除されました')）
```
**F-3**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ deleted book reviews still visible
（テスト本体: 別ユーザー投稿コメントを閲覧者で ->assertSee('公開レビュー本文')）
```
**F-4**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ deleted book hides buttons
（テスト本体: ->assertDontSee('編集')。BookPolicy::update/delete が ! $book->trashed() を含むため @can が偽）
```
**F-5**
証拠:
```
$ git diff 6a346be -- resources/views/books/show.blade.php  （お気に入りボタンのガード追記）
                                 <!-- お気に入りボタン -->
+                                @if(! $book->trashed())
                                 @auth
                                     @if(Auth::user()->favoriteBooks->contains($book->id))
 ...（@auth ブロック全体を囲む）...
                                 @endauth
+                                @endif
```
**F-6**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ deleted book hides buttons
$ git diff 6a346be -- resources/views/books/show.blade.php  （投稿フォームのガードと差し替え文言）
+                        @if(! $book->trashed())
                         @auth
                             <!-- レビュー投稿フォーム -->
 ...
                         @endauth
+                        @else
+                            <p class="mb-6 text-gray-600">削除済みの書籍にはレビューを投稿できません</p>
+                        @endif
（テスト本体: ->assertSee('削除済みの書籍にはレビューを投稿できません')）
```
**F-7**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ deleted book hides buttons        （owner視点で ->assertSee('復元する')）
  ✓ restore authorization             （other が patch(books.restore)->assertForbidden()）
$ git diff 6a346be -- resources/views/books/show.blade.php  （復元ボタン追記）
+                                @can('restore', $book)
+                                    <form action="{{ route('books.restore', $book) }}" method="POST" novalidate>
+                                        @csrf
+                                        @method('PATCH')
+                                        <button type="submit" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded">
+                                            復元する
+                                        </button>
+                                    </form>
+                                @endcan
```
**F-8**
証拠:
```
$ git diff 6a346be --name-only -- resources/
resources/views/books/show.blade.php

$ git diff 6a346be -- resources/views/books/show.blade.php
（変更ブロックは以下4箇所のみ。他のマークアップ・クラス名・レイアウトに追加/削除行なし）
  @@ -12,6 +12,11 @@   ... @if ($book->trashed()) 「この本は削除されました」バナー
  @@ -29,6 +34,7 @@ / @@ -56,6 +62,7 @@   ... お気に入り@authを @if(! $book->trashed()) で囲む
  @@ -89,6 +96,15 @@   ... @can('restore',$book) 「復元する」PATCHフォーム
  @@ -97,6 +113,7 @@ / @@ -138,6 +155,9 @@   ... レビュー投稿@authを @if(! $book->trashed()) で囲み @else に差し替え文言
```

#### G. 復元（restore）
**G-1**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ restore book
$ grep -n "書籍を復元しました" app/Http/Controllers/BookController.php
115:        return redirect()->route('books.show', $book)->with('success', '書籍を復元しました');
（テスト本体: owner patch(books.restore)->assertRedirect(books.show)->assertSessionHas('success','書籍を復元しました') / 復元後 get('/books')->assertSee($title) / get('/ranking')->assertSee($title)）
```
**G-2**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ restore book
（テスト本体: assertNull($book->fresh()->deleted_at)）
```
**G-3**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ restore authorization
（テスト本体: other patch(books.restore)->assertForbidden() / deleted_at 残存）
```
**G-4**
証拠:
```
$ grep -n trashed app/Policies/BookPolicy.php
15:        return $user->id === $book->user_id && ! $book->trashed();
23:        return $user->id === $book->user_id && ! $book->trashed();
31:        return $user->id === $book->user_id && $book->trashed();
（restore(31) は $book->trashed() を条件に含む。未削除では偽を返す）
```

#### H. コード品質・スコープ厳守
**H-1**
証拠:
```
$ sail bin pint --test
  ──────────────────────────────────────────────────────────────────── Laravel
    PASS   ......................................................... 107 files
```
**H-2**
証拠:
```
$ grep -rn "\$this->validate(|->validate()" app/Http/Controllers/
（一致なし。grep 終了コード=1）
BookController::store(StoreBookRequest $request) / update(UpdateBookRequest $request, Book $book)
```
**H-3**
証拠:
```
$ grep -n "authorize(" app/Http/Controllers/BookController.php
72:        $this->authorize('update', $book);
84:        $this->authorize('update', $book);
99:        $this->authorize('delete', $book);
111:        $this->authorize('restore', $book);
$ grep -n trashed app/Policies/BookPolicy.php
15/23/31: $user->id === $book->user_id ... (update/delete/restore)
（コントローラー内に if($book->user_id!==Auth::id()) 等の手書き判定の grep 一致なし）
```
**H-4**
証拠:
```
$ grep -oE "public function [a-zA-Z]+" app/Http/Controllers/BookController.php
public function index / create / store / show / edit / update / destroy / restore
（発注書§0の8アクションのみ。追加アクション・追加バリデーション項目なし）
```
**H-5**
証拠:
```
$ git diff 6a346be -- database/migrations/ | wc -l
0
```
**H-6**
証拠:
```
$ grep -nE "^\| " QUESTIONS.md
10:| （例）8/26 | 02_書籍CRUD | ...（雛形の例示行）
11:| 2026-09-02 | 05_公開APIシーディング | ...（走行⑤で記録）
（走行②起因の実記録行なし。10行目は雛形の（例）行）
```

---

## 走行③ — レビューお気に入りいいね
対象検品表: `docs/03_検品表/03_レビューお気に入りいいね.md`

### 検品前に実行するコマンド（出力）
```
$ git branch --show-current
feature/06-basic-test

$ sail artisan route:list --path=reviews
  POST      books/{book}/reviews ...... reviews.store › ReviewController@store
  PUT       reviews/{review} ........ reviews.update › ReviewController@update
  DELETE    reviews/{review} ...... reviews.destroy › ReviewController@destroy
  GET|HEAD  reviews/{review}/edit ....... reviews.edit › ReviewController@edit
  POST      reviews/{review}/like ....... reviews.like › ReviewController@like

$ sail artisan route:list --path=favorites
  POST      books/{book}/favorites favorites.toggle › FavoriteController@toggle
  GET|HEAD  favorites ............ favorites.index › FavoriteController@index

$ sail bin pint --test
    PASS   ......................................................... 107 files
```

### 行ごとの証拠

#### A. ルーティング
**A-1**
証拠:
```
$ sail artisan route:list --path=reviews
  POST      books/{book}/reviews ...... reviews.store › ReviewController@store
  PUT       reviews/{review} ........ reviews.update › ReviewController@update
  DELETE    reviews/{review} ...... reviews.destroy › ReviewController@destroy
  GET|HEAD  reviews/{review}/edit ....... reviews.edit › ReviewController@edit
  POST      reviews/{review}/like ....... reviews.like › ReviewController@like
```
**A-2**
証拠:
```
$ sail artisan route:list --path=favorites
  POST      books/{book}/favorites favorites.toggle › FavoriteController@toggle
  GET|HEAD  favorites ............ favorites.index › FavoriteController@index
```
**A-3**
証拠:
```
$ ls app/Http/Controllers/
Api  BookController.php  Controller.php  FavoriteController.php  GenreController.php  RankingController.php  ReviewController.php
（LikeController.php は存在しない）
$ grep -oE "public function [a-zA-Z]+" app/Http/Controllers/ReviewController.php
public function store / edit / update / destroy / like
```
**A-4**
証拠:
```
$ ls app/Http/Controllers/  ->  FavoriteController.php が存在
$ grep -oE "public function [a-zA-Z]+" app/Http/Controllers/FavoriteController.php
public function index / toggle
```
**A-5**
証拠:
```
$ grep -n "middleware('auth')" routes/web.php  および reviews./favorites. の定義位置
  reviews.store/edit/update/destroy/like・favorites.index/toggle はいずれも
  Route::middleware('auth')->group(function () { ... }); ブロック内に定義されている。
```

#### B. レビュー投稿・編集・削除
**B-1**
証拠:
```
$ sail artisan test --filter=ReviewTest
  ✓ store review
  ✓ store review without comment
$ grep -n "レビューを投稿しました" app/Http/Controllers/ReviewController.php
20:        return redirect()->route('books.show', $book)->with('success', 'レビューを投稿しました');
（テスト本体: assertRedirect(route('books.show',$book))->assertSessionHas('success','レビューを投稿しました') / comment省略時 assertDatabaseHas('reviews',['comment'=>null])）
```
**B-2**
証拠:
```
$ sail artisan test --filter=ReviewTest
  ✓ store review requires rating
$ grep -nE "between|required|max:1000|評価|コメント" app/Http/Requests/StoreReviewRequest.php
  'rating' => ['required', 'integer', 'between:1,5'],
  'comment'=> ['nullable', 'string', 'max:1000'],
  'rating.required' => '評価を選択してください',
  'rating.between'  => '評価は1〜5の範囲で選択してください',
（テスト本体: rating欠落 -> assertSessionHasErrors('rating')）
```
**B-3**
証拠:
```
$ sail artisan test --filter=ReviewTest
  ✓ edit authorization
  ✓ delete authorization
（テスト本体: owner get(reviews.edit)->assertOk / other ->assertForbidden / other put/delete ->assertForbidden）
```
**B-4**
証拠:
```
$ sail artisan test --filter=ReviewTest
  ✓ review operations on deleted book
$ sail artisan test --filter=ReviewTest（Unit）
  ✓ review book uses with trashed
（Review::book() は belongsTo(Book::class)->withTrashed()。書籍削除後 owner get(reviews.edit)->assertOk()）
```
**B-5**
証拠:
```
$ sail artisan test --filter=ReviewTest
  ✓ review delete cascades likes
（テスト本体: 削除前 review_likes count=1 -> destroy 後 count=0）
```
**B-6**
証拠:
```
$ grep -n "review->book" app/Http/Controllers/ReviewController.php
（destroy: $book = $review->book; を delete() 前に退避 -> redirect(books.show,$book)）
  52:        $this->authorize('delete', $review);
  55:        $book = $review->book;
  56:        $review->delete();
  57:        return redirect()->route('books.show', $book)->with('success', 'レビューを削除しました');
$ sail artisan test --filter=ReviewTest
  ✓ delete authorization   （assertRedirect(route('books.show',$review->book))）
```

#### C. いいねトグル
**C-1**
証拠:
```
$ sail artisan test --filter=ReviewLikeTest
  ✓ like toggle on off
（テスト本体: 1回目 post(reviews.like)->assertDatabaseHas('review_likes',...) / 2回目 ->assertDatabaseMissing(...)）
```
**C-2**
証拠:
```
$ grep -nA1 "public function like" app/Http/Controllers/ReviewController.php
  public function like(Review $review)
      Auth::user()->likedReviews()->toggle($review);
      return back();
（with() を付けていない＝flashを設定しない）
```
**C-3**
証拠:
```
$ sail artisan test --filter=ReviewLikeTest
  ✓ like on deleted book review
（テスト本体: book削除後 post(reviews.like) -> review_likes count=1）
```
**C-4**
証拠:
```
routes/web.php: reviews.like は Route::middleware('auth')->group 内。
$ sail artisan test --filter=ScreenAccessTest
  ✓ guest redirected from auth pages
（同 auth ミドルウェアにより未認証は /login へ）
```

#### D. お気に入りトグル・一覧
**D-1**
証拠:
```
$ sail artisan test --filter=FavoriteTest
  ✓ toggle on
（テスト本体: post(favorites.toggle)->assertDatabaseHas('favorites',['user_id'=>..,'book_id'=>..])）
```
**D-2**
証拠:
```
$ sail artisan test --filter=FavoriteTest
  ✓ toggle off
（テスト本体: 事前attach後 post -> assertDatabaseMissing('favorites',...)）
```
**D-3**
証拠:
```
$ sail artisan test --filter=FavoriteTest
  ✓ toggle has no flash
$ grep -nA1 "public function toggle" app/Http/Controllers/FavoriteController.php
  public function toggle(Book $book)
      Auth::user()->favoriteBooks()->toggle($book);
      return back();
（テスト本体: ->assertSessionMissing('success')）
```
**D-4**
証拠:
```
$ curl -s -i $B/favorites
HTTP/1.1 302 Found
Location: http://localhost:8022/login
$ sail artisan test --filter=FavoriteTest
  ✓ guest redirected from favorites
```
**D-5**
証拠:
```
$ sail artisan test --filter=FavoriteTest
  ✓ index shows only own favorites
（テスト本体: ->assertSee('マイお気に入り本')->assertDontSee('他人のお気に入り本')）
```
**D-6**
証拠:
```
$ grep -n "favoriteBooks" app/Http/Controllers/FavoriteController.php
15:        $books = Auth::user()->favoriteBooks()->latest('books.created_at')->paginate(10);
```

#### E. 削除済み書籍との関係
**E-1**
証拠:
```
favoriteBooks() 関連先 Book は SoftDeletes を持つ（app/Models/Book.php: use SoftDeletes）。
$ sail artisan test --filter=BookCrudTest
  ✓ deleted book excluded from listings   （getJson/一覧系の除外を実行）
```
**E-2**
証拠:
```
$ sail artisan tinker --execute="echo DB::table('book_genre')->where('book_id',11)->count();"  （AP05削除後）
2
（favorites.book_id は cascadeOnDelete だが SoftDelete下で物理DELETE未発行のため中間行は保持。book_genre で同挙動を実測。CLAUDE.md§9-4）
```
**E-3**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ restore book   （復元後 get('/books')->assertSee($title)。SoftDelete標準除外の解除）
（E-1 のSoftDelete除外 + restoreでdeleted_at=null により一覧クエリ対象へ復帰）
```

#### F. コード品質・スコープ厳守
**F-1**
証拠:
```
$ sail bin pint --test
    PASS   ......................................................... 107 files
```
**F-2**
証拠:
```
$ ls app/Http/Requests/ | grep Review
StoreReviewRequest.php
UpdateReviewRequest.php
$ grep -rn "\$this->validate(|->validate()" app/Http/Controllers/ReviewController.php   （一致なし）
```
**F-3**
証拠:
```
$ grep -n "authorize(" app/Http/Controllers/ReviewController.php
28:        $this->authorize('update', $review);
40:        $this->authorize('update', $review);
52:        $this->authorize('delete', $review);
```
**F-4**
証拠:
```
$ grep -n "trashed" app/Policies/ReviewPolicy.php
（一致なし。grep 終了コード=1）
```
**F-5**
証拠:
```
$ grep -oE "public function [a-zA-Z]+" app/Http/Controllers/ReviewController.php app/Http/Controllers/FavoriteController.php
ReviewController: store / edit / update / destroy / like
FavoriteController: index / toggle
```
**F-6**
証拠:
```
$ git diff 6a346be -- database/migrations/ | wc -l
0
```
**F-7**
証拠:
```
$ grep -nE "^\| " QUESTIONS.md
（走行③起因の記録行なし）
```

---

## 走行④ — ジャンルランキング
対象検品表: `docs/03_検品表/04_ジャンルランキング.md`

### 検品前に実行するコマンド（出力）
```
$ git branch --show-current
feature/06-basic-test

$ sail artisan route:list --path=genres
  GET|HEAD   genres ................ genres.index › GenreController@index
  POST       genres ................ genres.store › GenreController@store
  GET|HEAD   genres/create ....... genres.create › GenreController@create
  GET|HEAD   genres/{genre} .......... genres.show › GenreController@show
  PUT|PATCH  genres/{genre} ...... genres.update › GenreController@update
  DELETE     genres/{genre} .... genres.destroy › GenreController@destroy
  GET|HEAD   genres/{genre}/edit ..... genres.edit › GenreController@edit

$ sail artisan route:list --path=ranking
  GET|HEAD   ranking ............. ranking.index › RankingController@index

$ sail bin pint --test
    PASS   ......................................................... 107 files
```

### 行ごとの証拠

#### A. ルーティング
**A-1**
証拠:
```
$ sail artisan route:list --path=genres   （上記の7本 index/create/store/show/edit/update/destroy）
```
**A-2**
証拠:
```
$ sail artisan route:list --path=ranking
  GET|HEAD   ranking ............. ranking.index › RankingController@index
$ curl -s -i $B/ranking | head -1
HTTP/1.1 200 OK        （未ログインで200＝auth未適用）
```
**A-3**
証拠:
```
$ ls app/Policies/
BookPolicy.php
ReviewPolicy.php
（GenrePolicy.php は存在しない）
```

#### B. ジャンル一覧・詳細
**B-1**
証拠:
```
$ grep -n "withCount('books')" app/Http/Controllers/GenreController.php
  Genre::withCount('books')->orderBy('id')->get()
$ sail artisan test --filter=GenreTest
  ✓ genre screens render   （get(genres.index)->assertOk()->assertSee('技術書')）
```
**B-2**
証拠:
```
$ sail artisan test --filter=GenreTest
  ✓ destroy blocked when only trashed book attached
（withCount('books') は Book の SoftDeletes 影響で削除済みを数えない。削除済み書籍のみ紐付きでも削除はブロックされる＝§5）
```
**B-3**
証拠:
```
$ grep -n "books()->with('genres')" app/Http/Controllers/GenreController.php
  $genre->books()->with('genres')->latest('books.created_at')->paginate(10)
$ sail artisan test --filter=GenreTest
  ✓ genre screens render   （get(genres.show)->assertOk()->assertSee('ジャンル所属本')）
```
**B-4**
証拠:
```
$ grep -n "g->id === \$genre->id" resources/views/genres/show.blade.php
  <span class="... {{ $g->id === $genre->id ? 'bg-blue-200 text-blue-700' : '' }}">
（$books に with('genres') を渡している＝上記B-3のクエリ）
```

#### C. 登録・編集
**C-1**
証拠:
```
$ sail artisan test --filter=GenreTest
  ✓ store requires name
（テスト本体: post('/genres',[])->assertSessionHasErrors('name')）
```
**C-2**
証拠:
```
$ sail artisan test --filter=GenreTest
  ✓ store unique name
$ grep -n "既に登録されています" app/Http/Requests/StoreGenreRequest.php
  'name.unique' => 'このジャンル名は既に登録されています',
```
**C-3**
証拠:
```
$ sail artisan test --filter=GenreTest
  ✓ edit allowed for any authenticated user
（テスト本体: $creator と別の $other で get(genres.edit)->assertOk / put(genres.update)->assertRedirect(genres.index)->success）
```
**C-4**
証拠:
```
$ grep -n "ignore" app/Http/Requests/UpdateGenreRequest.php
  Rule::unique('genres', 'name')->ignore($this->route('genre'))
$ sail artisan test --filter=GenreTest
  ✓ edit allowed for any authenticated user   （自ジャンル名変更が一意性で弾かれず更新成功）
```

#### D. 削除制限（二重防御）
**D-1**
証拠:
```
$ sail artisan test --filter=GenreTest
  ✓ destroy blocked when books attached
$ grep -n "紐づく書籍が存在するため削除できません" app/Http/Controllers/GenreController.php
74:                ->with('error', 'このジャンルに紐づく書籍が存在するため削除できません');
（テスト本体: delete(genres.destroy)->assertRedirect(genres.index)->assertSessionHas('error',...)->assertDatabaseHas('genres',...)）
```
**D-2**
証拠:
```
$ sail artisan test --filter=GenreTest
  ✓ destroy success when no books
$ grep -n "ジャンルを削除しました" app/Http/Controllers/GenreController.php
79:        return redirect()->route('genres.index')->with('success', 'ジャンルを削除しました');
```
**D-3**
証拠:
```
$ grep -n "withTrashed" app/Http/Controllers/GenreController.php
72:        if ($genre->books()->withTrashed()->exists()) {
$ sail artisan test --filter=GenreTest
  ✓ destroy blocked when only trashed book attached
```
**D-4**
証拠:
```
$ sail artisan tinker --execute="try{ \$g=Genre::whereHas('books')->first(); echo 'target='.\$g->name; \$g->delete(); }catch(\Illuminate\Database\QueryException \$e){ echo get_class(\$e).': '.substr(\$e->getMessage(),0,90); }"
target=小説
Illuminate\Database\QueryException: SQLSTATE[23000]: Integrity constraint violation: 1451 Cannot delete or update a parent row
$ sail artisan test --filter=GenreTest
  ✓ db constraint prevents delete
```

#### E. ランキング
**E-1**
証拠:
```
$ grep -nE "withAvg|orderByDesc" app/Http/Controllers/RankingController.php
14:        $rankedBooks = Book::withAvg('reviews', 'rating')
17:            ->orderByDesc('reviews_avg_rating')
$ sail artisan test --filter=RankingTest
  ✓ ranking ordered by avg rating desc
```
**E-2**
証拠:
```
$ grep -n "whereHas('reviews')" app/Http/Controllers/RankingController.php
16:            ->whereHas('reviews')
$ sail artisan test --filter=RankingTest
  ✓ ranking excludes books without reviews
```
**E-3**
証拠:
```
$ sail artisan test --filter=RankingTest
  ✓ ranking excludes deleted books
（テスト本体: 削除後 get('/ranking')->assertDontSee(削除本)）
```
**E-4**
証拠:
```
$ sail artisan test --filter=BookCrudTest
  ✓ restore book   （復元後 get('/ranking')->assertSee($title)）
```
**E-5**
証拠:
```
$ curl -s -i $B/ranking | head -1
HTTP/1.1 200 OK
$ sail artisan test --filter=RankingTest
  ✓ ranking public
```
**E-6**
証拠:
```
$ grep -n "rankedBooks" app/Http/Controllers/RankingController.php
14:        $rankedBooks = Book::withAvg('reviews', 'rating')
22:        return view('ranking.index', compact('rankedBooks'));
```
**E-7**
証拠:
```
$ grep -n "number_format" resources/views/ranking/index.blade.php
50:            {{ number_format($book->reviews_avg_rating, 2) }}
62:            {{ number_format($book->reviews_avg_rating, 1) }}
（本文=第2位、右バッジ=第1位。コントローラーは生の平均値を渡す＝上記E-1のwithAvg）
```

#### F. コード品質・スコープ厳守
**F-1**
証拠:
```
$ sail bin pint --test
    PASS   ......................................................... 107 files
```
**F-2**
証拠:
```
$ ls app/Http/Requests/ | grep Genre
StoreGenreRequest.php
UpdateGenreRequest.php
$ grep -rn "\$this->validate(|->validate()" app/Http/Controllers/GenreController.php   （一致なし）
```
**F-3**
証拠:
```
$ grep -nE "withAvg|withCount|whereHas|with\('reviews'\)" app/Http/Controllers/RankingController.php
14:        $rankedBooks = Book::withAvg('reviews', 'rating')
15:            ->withCount('reviews')
16:            ->whereHas('reviews')
（with('reviews') の一致なし）
```
**F-4**
証拠:
```
$ grep -oE "public function [a-zA-Z]+" app/Http/Controllers/GenreController.php app/Http/Controllers/RankingController.php
GenreController: index / create / store / show / edit / update / destroy
RankingController: index
（GenrePolicy未作成＝A-3）
```
**F-5**
証拠:
```
$ git diff 6a346be -- database/migrations/ | wc -l
0
```
**F-6**
証拠:
```
$ grep -nE "^\| " QUESTIONS.md
（走行④起因の記録行なし）
```

---

## 走行⑤ — 公開APIシーディング
対象検品表: `docs/03_検品表/05_公開APIシーディング.md`

### 検品前に実行するコマンド（出力）
```
$ git branch --show-current
feature/06-basic-test

$ sail artisan migrate:fresh --seed
   INFO  Seeding database.
  Database\Seeders\UserSeeder ........................................ RUNNING
  Database\Seeders\UserSeeder .................................... 902 ms DONE
  Database\Seeders\GenreSeeder ....................................... RUNNING
  Database\Seeders\GenreSeeder .................................... 28 ms DONE
  Database\Seeders\BookSeeder ........................................ RUNNING
  Database\Seeders\BookSeeder ..................................... 90 ms DONE
  Database\Seeders\ReviewSeeder ...................................... RUNNING
  Database\Seeders\ReviewSeeder ................................... 89 ms DONE
  Database\Seeders\FavoriteSeeder .................................... RUNNING
  Database\Seeders\FavoriteSeeder ................................. 56 ms DONE
  Database\Seeders\ReviewLikeSeeder .................................. RUNNING
  Database\Seeders\ReviewLikeSeeder .............................. 130 ms DONE

$ sail artisan tinker --execute="foreach(['users','genres','books','reviews','favorites','review_likes'] as $t){echo $t.'='.DB::table($t)->count().PHP_EOL;}"
users=5
genres=10
books=11
reviews=32
favorites=19
review_likes=48

$ sail artisan route:list --path=api
  GET|HEAD   api/v1/books ..... books.index › Api\V1\BookController@index
  POST       api/v1/books ..... books.store › Api\V1\BookController@store
  GET|HEAD   api/v1/books/{book} books.show › Api\V1\BookController@show
  PUT|PATCH  api/v1/books/{book} books.update › Api\V1\BookController@update
  DELETE     api/v1/books/{book} books.destroy › Api\V1\BookController@destroy
                                                            Showing [5] routes

$ sail bin pint --test
    PASS   ......................................................... 107 files
```

### 行ごとの証拠

#### A. シーディング（採点直結）
**A-1**
証拠:
```
$ sail artisan migrate:fresh --seed
   INFO  Seeding database.
  Database\Seeders\UserSeeder ... DONE
  Database\Seeders\GenreSeeder ... DONE
  Database\Seeders\BookSeeder ... DONE
  Database\Seeders\ReviewSeeder ... DONE
  Database\Seeders\FavoriteSeeder ... DONE
  Database\Seeders\ReviewLikeSeeder ... DONE
（エラー出力なし。全6Seeder RUNNING/DONE）
```
**A-2**
証拠:
```
$ sail artisan tinker --execute="foreach(['users','genres','books','reviews'] as $t){echo $t.'='.DB::table($t)->count().PHP_EOL;}"
users=5
genres=10
books=11
reviews=32
$ sail artisan test --filter=SeederTest
  ✓ seed counts   （assertSame(5/10/11/32)）
```
**A-3**
証拠:
```
$ sail artisan db:seed   （2回目実行）
  Database\Seeders\ReviewLikeSeeder .............................. 138 ms DONE
$ sail artisan tinker --execute="foreach(['users','genres','books','reviews'] as $t){echo $t.'='.DB::table($t)->count().PHP_EOL;}"
users=5
genres=10
books=11
reviews=64
（users/genres/books は firstOrCreate により不変=5/10/11。reviews は ReviewSeeder が create 使用のため 32->64）
$ sail artisan test --filter=SeederTest
  ✓ seed is idempotent   （2回seed後 users/genres/books=5/10/11 を assertSame）
```
**A-4**
証拠:
```
$ sail artisan tinker（users）
1 山田太郎 yamada@example.com
2 鈴木花子 suzuki@example.com
3 田中一郎 tanaka@example.com
4 佐藤美咲 sato@example.com
5 高橋健太 takahashi@example.com
$ sail artisan tinker（genres 投入順=id順）
1:小説 2:ビジネス 3:技術書 4:自己啓発 5:エッセイ 6:歴史 7:科学 8:芸術 9:料理 10:旅行
$ sail artisan tinker --execute="echo App\Models\Book::find(2)->genres->pluck('name')->implode(',');"
ビジネス,自己啓発
（books は BookSeeder に11件を シート9記載のタイトル/著者/ISBN/出版日/ジャンルで定義。book_id=2「人を動かす」=ビジネス,自己啓発）
```
**A-5**
証拠:
```
$ sail artisan tinker --execute="echo App\Models\Book::find(3)->title; echo App\Models\Genre::find(3)->name;"
book3=リーダブルコード
genre3=技術書
```
**A-6**
証拠:
```
$ sail artisan tinker（image_url 1/3/11）
book1 image_url=https://placehold.co/200x300/e2e8f0/475569?text=1
book3 image_url=https://placehold.co/200x300/e2e8f0/475569?text=3
book11 image_url=https://placehold.co/200x300/e2e8f0/475569?text=11
（各書籍に description も設定。BookSeeder の firstOrCreate 属性）
```
**A-7**
証拠:
```
$ sail artisan tinker --execute="echo DB::table('review_likes as rl')->join('reviews as r','r.id','rl.review_id')->whereColumn('r.user_id','rl.user_id')->count();"
self_likes=0
（自分のレビューへのいいねは0件。ReviewLikeSeeder は likeCount=reviewIndex%4（0〜3）で投稿者本人を除外）
```

#### B. AP01 書籍一覧
**B-1**
証拠:
```
$ curl -s -i "$B/api/v1/books"
HTTP/1.1 200 OK

{"data":[{"id":1,"title":"吾輩は猫である",...}], ...
"meta":{"current_page":1,"last_page":2,"per_page":10,"total":11, ...}}
（data 10件、meta.per_page=10、total=11）
```
**B-2**
証拠:
```
$ curl -s -i "$B/api/v1/books?per_page=101"
HTTP/1.1 422 Unprocessable Content

{"message":"入力内容に誤りがあります。","errors":{"per_page":["取得件数は1〜100の範囲で指定してください"]}}
（"取得件数は1〜100の範囲で指定してください"）
$ sail artisan test --filter=BookApiTest
  ✓ index validation error   （per_page=101 / page=0 / genre_id=9999 いずれも 422）
```
**B-3**
証拠:
```
$ curl -s "$B/api/v1/books" | (data[0]のキー)
["id","title","author","isbn","published_date","image_url","average_rating","reviews_count","genres"]
（description キーなし）
```
**B-4**
証拠:
```
（上記 data[0] キーに genres / average_rating / reviews_count を含む）
$ curl -s "$B/api/v1/books?genre_id=3"（先頭要素）
{"id":3,...,"average_rating":4,"reviews_count":3,"genres":[{"id":3,"name":"技術書"}]}
```
**B-5**
証拠:
```
$ curl -s -i "$B/api/v1/books?keyword=zzzzz"
HTTP/1.1 200 OK
{"data":[],"links":{"first":"http:\/\/localhost:8022\/api\/v1\/books?keyword=zzzzz&page=1", ...
```
**B-6**
証拠:
```
$ curl -s "$B/api/v1/books?genre_id=3" | grep -o '"first":"[^"]*"'
"first":"http:\/\/localhost:8022\/api\/v1\/books?genre_id=3&page=1"
（genre_id が links に引き継がれる）
```

#### C. AP02 書籍詳細
**C-1**
証拠:
```
$ curl -s -i "$B/api/v1/books/3"
HTTP/1.1 200 OK

{"data":{"id":3,"title":"リーダブルコード",...,"reviews":[
{"user_name":"田中一郎","rating":5,"comment":"読み応えがあり、満足しています。","created_at":"2026-09-02T11:55:53+09:00"},
{"user_name":"佐藤美咲","rating":3,"comment":"...","created_at":"2026-09-02T11:55:53+09:00"},
{"user_name":"高橋健太","rating":4,"comment":"...","created_at":"2026-09-02T11:55:53+09:00"}]}}
```
**C-2**
証拠:
```
$ sail artisan test --filter=BookApiTest
  ✓ show success with null comment
（テスト本体: comment=null と comment='あり' の2件で assertJsonCount(2,'data.reviews')。ReviewResource は 'comment'=>$this->comment で常に出力）
```
**C-3**
証拠:
```
（上記 C-1 の created_at）
"created_at":"2026-09-02T11:55:53+09:00"
$ grep -n toIso8601String app/Http/Resources/ReviewResource.php
  'created_at' => $this->created_at->toIso8601String(),
```
**C-4**
証拠:
```
$ curl -s -i "$B/api/v1/books/99999"
HTTP/1.1 404 Not Found

{"message":"指定された書籍が見つかりません。"}
（errors キーなし）
```
**C-5**
証拠:
```
$ curl -s -i -X DELETE "$B/api/v1/books/11"   ->  HTTP/1.1 204 No Content
$ curl -s -i "$B/api/v1/books/11"
HTTP/1.1 404 Not Found

{"message":"指定された書籍が見つかりません。"}
$ sail artisan test --filter=BookApiTest
  ✓ show soft deleted returns 404
```

#### D. AP03 書籍登録
**D-1**
証拠:
```
$ curl -s -i -X POST "$B/api/v1/books" -d '{"title":"プログラマー脳",...,"genres":[3],"user_id":3}'
HTTP/1.1 201 Created

{"data":{"id":12,"title":"プログラマー脳",...,"average_rating":0,"reviews_count":0,"genres":[{"id":3,"name":"技術書"}]}}
（data のキーに reviews なし）
```
**D-2**
証拠:
```
（上記 D-1 の data のキー）
["id","title","author","isbn","published_date","description","image_url","average_rating","reviews_count","genres"]
（user_id キーなし）
```
**D-3**
証拠:
```
$ curl -s -i -X POST "$B/api/v1/books" -d '{}'
HTTP/1.1 422 Unprocessable Content

{"message":"入力内容に誤りがあります。","errors":{"title":["タイトルを入力してください"],"author":[...],"isbn":["ISBNを入力してください"],"published_date":[...],"genres":[...],"user_id":["登録者IDを指定してください"]}}
```
**D-4**
証拠:
```
$ grep -n "genres()->sync" app/Http/Controllers/Api/V1/BookController.php
  $book->genres()->sync($request->genres);
$ sail artisan test --filter=BookApiTest
  ✓ store success and validation   （201 + assertDatabaseHas('books',...)）
```

#### E. AP04 書籍更新
**E-1**
証拠:
```
$ curl -s -i -X PUT "$B/api/v1/books/1" -d '{"title":"吾輩は猫である(改)",...}'
HTTP/1.1 200 OK

{"data":{"id":1,"title":"吾輩は猫である(改)","isbn":"9784101010014",...}}
```
**E-2**
証拠:
```
（上記 E-1 は book1 の元ISBN 9784101010014 のまま PUT -> HTTP 200）
$ grep -n "ignore" app/Http/Requests/Api/V1/UpdateApiBookRequest.php
  Rule::unique('books', 'isbn')->ignore($this->route('book'))
$ sail artisan test --filter=BookApiTest
  ✓ update success ignores own isbn
```
**E-3**
証拠:
```
$ curl -s -i -X PUT "$B/api/v1/books/99999" -d '{...正常データ...}'   ->  HTTP/1.1 404 Not Found
$ curl -s -i -X PUT "$B/api/v1/books/2" -d '{}'                       ->  HTTP/1.1 422 Unprocessable Content
$ sail artisan test --filter=BookApiTest
  ✓ update not found and validation
```

#### F. AP05 書籍削除
**F-1**
証拠:
```
$ curl -s -i -X DELETE "$B/api/v1/books/11"
HTTP/1.1 204 No Content
（本文なし）
```
**F-2**
証拠:
```
$ sail artisan tinker --execute="\$b=Book::withTrashed()->find(11); echo 'deleted_at='.\$b->deleted_at;"
deleted_at=2026-09-02 12:07:36
$ sail artisan test --filter=BookApiTest
  ✓ destroy soft deletes   （assertSoftDeleted('books')+assertDatabaseHas('books')）
```
**F-3**
証拠:
```
$ curl -s -i -X DELETE "$B/api/v1/books/99999"
HTTP/1.1 404 Not Found
```
**F-4**
証拠:
```
$ sail artisan tinker --execute="echo 'reviews='.Review::where('book_id',11)->count().' book_genre='.DB::table('book_genre')->where('book_id',11)->count();"   （AP05削除後）
reviews=3 book_genre=2
```

#### G. コード品質・スコープ厳守
**G-1**
証拠:
```
$ sail bin pint --test
    PASS   ......................................................... 107 files
```
**G-2**
証拠:
```
$ ls app/Http/Requests/Api/V1/
ApiFormRequest.php  IndexBookRequest.php  StoreApiBookRequest.php  UpdateApiBookRequest.php
$ grep -n "failedValidation\|入力内容に誤りがあります" app/Http/Requests/Api/V1/ApiFormRequest.php
  protected function failedValidation(Validator $validator): void
      'message' => '入力内容に誤りがあります。',
```
**G-3**
証拠:
```
$ grep -n "authorize(" app/Http/Controllers/Api/V1/BookController.php
（一致なし。grep 終了コード=1）
```
**G-4**
証拠:
```
$ grep ^namespace app/Http/Controllers/BookController.php app/Http/Controllers/Api/V1/BookController.php
app/Http/Controllers/BookController.php:namespace App\Http\Controllers;
app/Http/Controllers/Api/V1/BookController.php:namespace App\Http\Controllers\Api\V1;
```
**G-5**
証拠:
```
$ grep -oE "public function [a-zA-Z]+" app/Http/Controllers/Api/V1/BookController.php
public function index / show / store / update / destroy
（Handler の api/* 404差し替えは発注書§7指定。追加ロジックなし）
```
**G-6**
証拠:
```
$ git diff 6a346be -- database/migrations/ | wc -l
0
$ ls database/migrations/ | grep personal_access
（一致なし。grep 終了コード=1）
```
**G-7**
証拠:
```
$ grep -nE "^\| " QUESTIONS.md
11:| 2026-09-02 | 05_公開APIシーディング | `App\Models\Book` に published_date の date cast が無い（走行①漏れ）。
   発注書05§5は $this->published_date->format('Y-m-d') 前提だが、cast追加は books/show.blade の
   {{ $book->published_date }} 表示を変え frozen Blade 契約（§0）に反する。
   | (a)§0優先でモデル未変更・Resource側で Carbon::parse(...)->format('Y-m-d')（採用）
     (b)モデルに date:Y-m-d cast追加 |
（上記B/C/D/Eの published_date 実測は "1905-01-01" "2012-06-23" 形式＝採用案(a)の結果）
```

---

## 走行⑥ — 基本テスト
対象検品表: `docs/03_検品表/06_基本テスト.md`

### 検品前に実行するコマンド（出力）
```
$ git branch --show-current
feature/06-basic-test

$ sail artisan test
  Tests:    72 passed (223 assertions)
  Duration: 2.65s

$ sail artisan test --coverage
  Http/Controllers/Api/V1/BookController ...................... 86.1%
  Http/Controllers/BookController ............................. 94.3%
  Http/Controllers/GenreController ........................... 100.0%
  Http/Controllers/ReviewController .......................... 100.0%
  Http/Controllers/FavoriteController ........................ 100.0%
  Http/Controllers/RankingController ......................... 100.0%
  Http/Requests/*(全10本) .................................... 100.0%
  Http/Resources/*(4本) ...................................... 100.0%
  Policies/BookPolicy / ReviewPolicy ......................... 100.0%
  Models/Book 75.0% / Genre 100.0% / Review 100.0%
                                                    Total: 95.8 %

$ sail bin pint --test
    PASS   ......................................................... 107 files
```

### 行ごとの証拠

#### A. 実行結果
**A-1**
証拠:
```
$ sail artisan test
  Tests:    72 passed (223 assertions)
  Duration: 2.65s
（FAIL 行なし）
```
**A-2**
証拠:
```
$ sail artisan test --coverage
                                                    Total: 95.8 %
```
**A-3**
証拠:
```
$ sail artisan test（1回目）  ->  Tests: 72 passed (223 assertions)
$ sail artisan test（2回目）  ->  Tests: 72 passed (223 assertions)
```

#### B. テストファイルの存在と観点網羅
**B-1**
証拠:
```
$ sail artisan test --filter=ScreenAccessTest
   PASS  Tests\Feature\ScreenAccessTest
  ✓ authenticated user redirected from guest pages   (F-A1)
  ✓ guest redirected from auth pages                 (F-A2)
  ✓ public pages accessible by guest                 (F-A3)
  ✓ intended url after login                         (F-A4)
  Tests:    4 passed (22 assertions)
```
**B-2**
証拠:
```
$ sail artisan test --filter=AuthTest
   PASS  Tests\Feature\AuthTest
  ✓ registration logs in and redirects   (F-A5)
  ✓ registration validation errors       (F-A6)
  ✓ login success                        (F-A7)
  ✓ login failure message                (F-A8)
  ✓ logout requires login again          (F-A9)
  Tests:    5 passed (24 assertions)
（F-A8 本体: session('errors')->first('email')==='メールアドレスまたはパスワードが正しくありません' を assert）
$ grep -n "メールアドレスまたはパスワードが正しくありません" lang/ja/auth.php
6:    'failed' => 'メールアドレスまたはパスワードが正しくありません',
```
**B-3**
証拠:
```
$ sail artisan test --filter=BookTest
   PASS  Tests\Unit\BookTest
  ✓ book belongs to user            (U-B1)
  ✓ book belongs to many genres     (U-B2)
  ✓ book has many reviews           (U-B3)
  ✓ reviews avg rating              (U-B4: 0件でnullも確認)
  ✓ soft delete exclusion           (U-B5)
  Tests:    5 passed (10 assertions)
```
**B-4**
証拠:
```
$ sail artisan test --filter=BookCrudTest
   PASS  Tests\Feature\BookCrudTest
  ✓ store book success               (F-B1)
  ✓ store validation errors          (F-B2)
  ✓ genre sync on store and update   (F-B3)
  ✓ edit authorization               (F-B4)
  ✓ delete authorization             (F-B5)
  ✓ deleted book excluded from listings   (F-B6)
  ✓ deleted book show page           (F-B7)
  ✓ deleted book hides buttons       (F-B8)
  ✓ deleted book reviews still visible    (F-B9)
  ✓ restore book                     (F-B10)
  ✓ restore authorization            (F-B11)
  ✓ deleted isbn uniqueness          (F-B12)
  ✓ reviews kept after book soft delete   (F-B13)
  Tests:    13 passed (44 assertions)
```
**B-5**
証拠:
```
$ sail artisan test --filter=ReviewTest（Unit）
   PASS  Tests\Unit\ReviewTest
  ✓ review belongs to book           (U-R1)
  ✓ review belongs to user           (U-R2)
  ✓ review liked by users            (U-R3)
  ✓ review book uses with trashed    (U-R4)
```
**B-6**
証拠:
```
$ sail artisan test --filter=ReviewTest（Feature）
   PASS  Tests\Feature\ReviewTest
  ✓ store review / store review without comment / store review requires rating   (F-R1)
  ✓ edit authorization               (F-R2)
  ✓ delete authorization             (F-R3)
  ✓ review delete cascades likes      (F-R4)
  ✓ review operations on deleted book (F-R5)
```
**B-7**
証拠:
```
$ sail artisan test --filter=ReviewLikeTest
   PASS  Tests\Feature\ReviewLikeTest
  ✓ like toggle on off               (F-L1)
  ✓ like on deleted book review       (F-L2)
  Tests:    2 passed (3 assertions)
```
**B-8**
証拠:
```
$ sail artisan test --filter=FavoriteTest（Unit）
   PASS  Tests\Unit\FavoriteTest
  ✓ favorite books toggle            (U-F1)
```
**B-9**
証拠:
```
$ sail artisan test --filter=FavoriteTest（Feature）
   PASS  Tests\Feature\FavoriteTest
  ✓ toggle on                        (F-F1)
  ✓ toggle off                       (F-F2)
  ✓ toggle has no flash              (F-F3)
  ✓ guest redirected from favorites  (F-F4)
  ✓ index shows only own favorites   (F-F5)
```
**B-10**
証拠:
```
$ sail artisan test --filter=GenreTest（Unit）
   PASS  Tests\Unit\GenreTest
  ✓ genre belongs to many books      (U-G1)
```
**B-11**
証拠:
```
$ sail artisan test --filter=GenreTest（Feature）
   PASS  Tests\Feature\GenreTest
  ✓ store requires name              (F-G1)
  ✓ store unique name                (F-G2)
  ✓ edit allowed for any authenticated user   (F-G3)
  ✓ destroy blocked when books attached       (F-G4)
  ✓ destroy blocked when only trashed book attached  (F-G4補足)
  ✓ destroy success when no books    (F-G5)
  ✓ db constraint prevents delete    (F-G6)
  ✓ genre screens render / store success  （画面・登録の追加カバー）
```
**B-12**
証拠:
```
$ sail artisan test --filter=RankingTest
   PASS  Tests\Feature\RankingTest
  ✓ ranking ordered by avg rating desc      (F-K1)
  ✓ ranking excludes books without reviews  (F-K2)
  ✓ ranking excludes deleted books          (F-K3)
  ✓ ranking public                          (F-K4)
  Tests:    4 passed (8 assertions)
```
**B-13**
証拠:
```
$ sail artisan test --filter=BookApiTest
   PASS  Tests\Feature\Api\BookApiTest
  ✓ index success default pagination  (F-P1)
  ✓ index validation error            (F-P2)
  ✓ show success with null comment     (F-P3)
  ✓ show not found                    (F-P4)
  ✓ show soft deleted returns 404      (F-P5)
  ✓ store success and validation       (F-P6)
  ✓ update success ignores own isbn    (F-P7)
  ✓ update not found and validation    (F-P8)
  ✓ destroy soft deletes              (F-P9)
  ✓ destroy not found                 (F-P10)
  Tests:    10 passed (28 assertions)
```
**B-14**
証拠:
```
$ sail artisan test --filter=SeederTest
   PASS  Tests\Feature\SeederTest
  ✓ seed counts        (F-P11)
  ✓ seed is idempotent (F-P12)
  Tests:    2 passed (7 assertions)
```

#### C. テストの質
**C-1**
証拠:
```
$ grep -nE "\$owner|\$other|\$creator" tests/Feature/BookCrudTest.php tests/Feature/ReviewTest.php tests/Feature/GenreTest.php  （抜粋）
BookCrudTest test_edit_authorization/test_delete_authorization/test_restore_authorization:
  $owner = User::factory()->create();  $other = User::factory()->create();
ReviewTest test_edit_authorization/test_delete_authorization:
  $owner = User::factory()->create();  $other = User::factory()->create();
GenreTest test_edit_allowed_for_any_authenticated_user:
  $creator = User::factory()->create();  $other = User::factory()->create();
（本人と別ユーザーを別インスタンスで生成）
```
**C-2**
証拠:
```
$ grep -nE "assertDatabaseHas|assertSoftDeleted|assertDatabaseMissing" tests/Feature/BookCrudTest.php tests/Feature/Api/BookApiTest.php  （抜粋）
BookCrudTest::test_reviews_kept_after_book_soft_delete: assertDatabaseHas('reviews',['id'=>$review->id,'book_id'=>$book->id])
BookApiTest::test_destroy_soft_deletes: assertSoftDeleted('books',...) / assertDatabaseHas('books',...)
```
**C-3**
証拠:
```
$ grep -nE "assertJsonPath|assertJsonMissingPath|assertJsonCount|first\('email'\)" tests/Feature/Api/BookApiTest.php tests/Feature/AuthTest.php  （抜粋3件）
BookApiTest::test_index_success_default_pagination: assertJsonCount(10,'data') / assertJsonPath('meta.per_page',10) / assertJsonPath('meta.total',15)
BookApiTest::test_store_success_and_validation: assertJsonPath('data.title',...) / assertJsonMissingPath('data.reviews') / assertJsonMissingPath('data.user_id')
AuthTest::test_login_failure_message: session('errors')->first('email') の文言を assertSame
```
**C-4**
証拠:
```
$ git show --stat f979e21   （走行⑥コミットの変更ファイル）
 tests/Feature/Api/BookApiTest.php / AuthTest.php / BookCrudTest.php / ExampleTest.php(削除) /
 FavoriteTest.php / GenreTest.php / RankingTest.php / ReviewLikeTest.php / ReviewTest.php /
 ScreenAccessTest.php / SeederTest.php / tests/Unit/BookTest.php / ExampleTest.php(削除) /
 FavoriteTest.php / GenreTest.php / ReviewTest.php
（走行⑥コミットは tests/ 配下のみ。app/・database/ への変更を含まない。published_date cast の走行①漏れは走行⑤時点で QUESTIONS.md に記録済み）
```

#### D. コード品質・スコープ厳守
**D-1**
証拠:
```
$ sail bin pint --test
    PASS   ......................................................... 107 files
```
**D-2**
証拠:
```
$ git show --stat f979e21   （変更対象は tests/ 配下のみ。Controller/Model/Policy/FormRequest/Resource/migration への差分なし）
（詳細ファイル一覧は上記 C-4）
```
**D-3**
証拠:
```
$ grep -nE "^\| " QUESTIONS.md
（走行⑥起因の記録行なし。11行目の走行⑤記録1件のみ。72テスト実行で新規の実装不一致は検出せず）
```
