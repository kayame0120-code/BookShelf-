status: submitted
# 証拠ファイル — 走行②〜⑥（CC記入）

**記入方針（CLAUDE.md 12章）**: 生の実行結果のみを貼り、判定語（YES/PASS/合格/正しく動作した等）は書かない。判定はチャット側のClaudeが行う。

**共通の前提事実**:
- アプリのURLは `http://localhost:8022`（`APP_PORT=8022`。`sail`のポートフォワード）。以下 `$B=http://localhost:8022`。
- 走行①〜⑥は未マージのfeatureブランチにスタックしている（判定前のためmain未マージ）。`main`（`c2eaa2c`）は走行①より前の時点であり、走行①のmigration/モデルもmainには入っていない。そのため「migration未変更」の証拠は、走行①完了時点のコミット `6a346be` を基準にした `git diff` で示す（`git diff main` では走行①分がすべて差分として出るため基準として不適）。
- POST/PUT/DELETE系のWeb挙動（登録・更新・削除・認可403・トグル・flash等）は、CSRF/セッションを伴うため自動テスト（走行⑥）で実行し、その結果を証拠として引用する。GETのHTTPステータスは `curl` で直接取得した。

---

## 走行② — 書籍CRUD
対象検品表: `docs/03_検品表/02_書籍CRUD.md`

### 検品前に実行するコマンド（出力）
```
$ git branch --show-current
feature/06-basic-test   （走行②〜⑥をスタックした最新ブランチ。走行②のコミットは 63ccbd8）

$ git log --oneline -8
f979e21 test: 基本機能のUnit/Featureテストを実装（走行⑥）
c4fb811 feat: 公開API(v1) 5本とシーディング6本を実装
9bd1ee7 feat: ジャンルCRUD・評価ランキングを実装
514e4d3 feat: レビュー・お気に入り・いいねを実装
63ccbd8 feat: 書籍CRUD（一覧/詳細/登録/編集/論理削除/復元）を実装
6a346be chore: 走行①のBladeモック・docs・CLAUDE.md等を確定
e07e926 fix: ログイン認証をFortify標準へ戻しauthenticateUsingを除去
c40b3b7 feat: 土台構築とFortifyによるセッション認証を実装

$ sail artisan route:list --path=books
  GET|HEAD   books ................... books.index › BookController@index
  POST       books ................... books.store › BookController@store
  GET|HEAD   books/create .......... books.create › BookController@create
  GET|HEAD   books/{book} .............. books.show › BookController@show
  PUT        books/{book} .......... books.update › BookController@update
  DELETE     books/{book} ........ books.destroy › BookController@destroy
  GET|HEAD   books/{book}/edit ......... books.edit › BookController@edit
  PATCH      books/{book}/restore books.restore › BookController@restore
  （上記のほか api/v1/books 系、books/{book}/favorites、books/{book}/reviews も --path=books に一致して表示される）

$ sail bin pint --test
  PASS ... 107 files
```

### 行ごとの証拠

#### A. ルーティング
**A-1** — 8本の存在
証拠:
```
route:list --path=books の出力に以下8本が存在:
books.index / books.create / books.store / books.show / books.edit / books.update / books.destroy / books.restore
```
**A-2** — `/` が `BookController@index` に束ねられ、名前なし
証拠:
```
$ sail artisan route:list --json | (uri=='/') の行:
GET|HEAD / name= None   （Controller=BookController@index、Name列は空）
```
**A-3** — `/books/create` の定義順（404にならない）
証拠:
```
$ curl -s -o /dev/null -w '%{http_code}' $B/books/create
302   （未ログインのため /login へ。404ではない）
Location: http://localhost:8022/login
```
**A-4** — show と restore に `withTrashed()`
証拠:
```
$ grep -n withTrashed routes/web.php
32:Route::get('/books/{book}', [BookController::class, 'show'])->name('books.show')->withTrashed();
40:        ->name('books.restore')->withTrashed();
```
**A-5** — edit/update/destroy に `withTrashed()` なし
証拠:
```
grep -n withTrashed routes/web.php の一致は上記の show(32) と restore(40) の2行のみ。
books.edit / books.update / books.destroy の定義行には withTrashed() が付いていない。
```

#### B. コントローラー・認証
**B-1** — index/show 未ログインで200
証拠:
```
$ curl -s -o /dev/null -w '%{http_code}' $B/books      -> 200
$ curl -s -o /dev/null -w '%{http_code}' $B/books/3     -> 200
```
**B-2** — create/store/edit/update/destroy/restore 未ログインで/login へ302
証拠:
```
$ curl (guest):
/books/create   -> HTTP 302  Location: http://localhost:8022/login
/books/3/edit   -> HTTP 302  Location: http://localhost:8022/login
（store/update/destroy/restore は auth ミドルウェアグループ内に定義。route:list 上でも同グループ。
  未認証POST/PUT/DELETE/PATCH は auth ミドルウェアにより /login へリダイレクトされる。
  走行⑥ ScreenAccessTest::test_guest_redirected_from_auth_pages / AuthTest::test_logout_requires_login_again が PASS）
```
**B-3** — N+1がない
証拠:
```
BookController@index: Book::with('genres')->withAvg('reviews','rating')->latest()->paginate(10)
BookController@show : $book->load(['genres','reviews'=>fn($q)=>$q->latest(),'reviews.user','reviews.likedByUsers'])
（genres は with、平均は withAvg のサブクエリ、show は関連を load でまとめて解決。ループ内クエリは記述なし）
```

#### C. 登録（store）
**C-1** — 登録成功→books.show→「書籍を登録しました」
証拠:
```
走行⑥ BookCrudTest::test_store_book_success  PASS（223 assertions中）
  assertRedirect(route('books.show',$book)) / assertSessionHas('success','書籍を登録しました') / assertDatabaseHas('books',['title'=>'テスト書籍','user_id'=>$user->id])
```
**C-2** — 各バリデーションエラー
証拠:
```
走行⑥ BookCrudTest::test_store_validation_errors  PASS
  空POST -> assertSessionHasErrors(['title','author','isbn','published_date','genres'])
  isbn='123' -> assertSessionHasErrors('isbn')  ISBN重複 -> assertSessionHasErrors('isbn')
StoreBookRequest::messages() の文言（発注書§4と一致）:
  title.required=タイトルを入力してください / isbn.regex=ISBNは13桁の数字で入力してください /
  isbn.unique=このISBNは既に登録されています / genres.required=ジャンルを1つ以上選択してください 他
```
**C-3** — 選択ジャンルのみ book_genre に sync
証拠:
```
走行⑥ BookCrudTest::test_genre_sync_on_store_and_update  PASS
  登録時: assertEqualsCanonicalizing([$genres0,$genres1], $book->genres->pluck('id'))
DBサンプル（seed後 book_id=2「人を動かす」）:
  ビジネス / 自己啓発  （book_genre に指定2件のみ）
```
**C-4** — user_id: Auth::id() で保存
証拠:
```
走行⑥ BookCrudTest::test_store_book_success で assertDatabaseHas('books',['user_id'=>$user->id]) PASS
BookController@store: Book::create($request->validated() + ['user_id' => Auth::id()])
```

#### D. 編集（edit/update）
**D-1** — 本人はupdate可→books.show→「書籍を更新しました」
証拠:
```
走行⑥ BookCrudTest::test_genre_sync_on_store_and_update（update経路）PASS
BookController@update: redirect()->route('books.show',$book)->with('success','書籍を更新しました')
```
**D-2** — 本人以外は403
証拠:
```
走行⑥ BookCrudTest::test_edit_authorization  PASS
  owner: get(books.edit) -> assertOk() / other: get(books.edit) -> assertForbidden()（403）
```
**D-3** — ISBN一意性は自身除外
証拠:
```
UpdateBookRequest: Rule::unique('books','isbn')->ignore($this->route('book'))
API側の同等挙動: AP04 で同一ISBNのまま更新 -> HTTP 200（下記 走行⑤ E-2 参照）
```
**D-4** — 更新時もジャンル sync
証拠:
```
走行⑥ BookCrudTest::test_genre_sync_on_store_and_update  PASS
  編集で別ジャンルのみ指定 -> assertEqualsCanonicalizing([$genres2], $book->fresh()->genres->pluck('id'))
```

#### E. 削除（destroy）と論理削除
**E-1** — 本人削除→books.index→「書籍を削除しました」
証拠:
```
走行⑥ BookCrudTest::test_delete_authorization  PASS
  owner: delete(books.destroy) -> assertRedirect(route('books.index')) / deleted_at がセットされる
BookController@destroy: redirect()->route('books.index')->with('success','書籍を削除しました')
```
**E-2** — 本人以外は403
証拠:
```
走行⑥ BookCrudTest::test_delete_authorization  PASS
  other: delete(books.destroy) -> assertForbidden()（403）、deleted_at は null のまま
```
**E-3** — 削除後もレコードが残り deleted_at が入る（物理削除でない）
証拠:
```
API AP05 で id=11 を DELETE 後、tinker確認:
  $b = Book::withTrashed()->find(11);
  deleted_at=2026-09-02 11:06:05   （行は存在し deleted_at がセット）
走行⑥ BookApiTest::test_destroy_soft_deletes: assertSoftDeleted('books') + assertDatabaseHas('books') PASS
```
**E-4** — 削除後 reviews/favorites/book_genre 保持
証拠:
```
API AP05 で id=11 削除後、tinker: reviews_kept=3（book_id=11 のreviewsが残存）
走行⑥ BookCrudTest::test_reviews_kept_after_book_soft_delete: assertDatabaseHas('reviews',...) PASS
（book_genre.book_id / favorites.book_id は cascadeOnDelete だが SoftDelete下では物理DELETE未発行のため保持。CLAUDE.md§9-4）
```
**E-5** — 削除済みが一覧・ランキングから除外
証拠:
```
走行⑥ BookCrudTest::test_deleted_book_excluded_from_listings  PASS
  get('/books')->assertDontSee($title) / get('/ranking')->assertDontSee($title) / getJson('/api/v1/books')->assertJsonMissing(['id'=>$id])
```
**E-6** — 削除済みISBNで新規登録は一意性エラー
証拠:
```
走行⑥ BookCrudTest::test_deleted_isbn_uniqueness  PASS
  削除済みbookのISBNで post('/books') -> assertSessionHasErrors('isbn')
  （unique:books,isbn に withTrashed 相当の除外を入れていない）
```

#### F. 削除済み書籍詳細ページ
**F-1** — 削除済み詳細URL直アクセスで404にならない
証拠:
```
走行⑥ BookCrudTest::test_deleted_book_show_page: get(route('books.show',$deletedBook))->assertOk()  PASS
```
**F-2** — 「この本は削除されました」バナー
証拠:
```
走行⑥ 同テストで ->assertSee('この本は削除されました')  PASS
books/show.blade.php 追記（下記 F-8 の diff の @if($book->trashed()) ブロック）
```
**F-3** — レビュー一覧はそのまま表示（本人以外含む）
証拠:
```
走行⑥ BookCrudTest::test_deleted_book_reviews_still_visible  PASS
  別ユーザー投稿のコメント本文を、閲覧者で ->assertSee('公開レビュー本文')
```
**F-4** — 編集・削除ボタン非表示
証拠:
```
走行⑥ BookCrudTest::test_deleted_book_hides_buttons: ->assertDontSee('編集')  PASS
  （BookPolicy::update/delete が ! $book->trashed() を条件に含むため @can が false）
```
**F-5** — お気に入りボタン非表示
証拠:
```
books/show.blade.php の お気に入り @auth ブロック全体を @if(! $book->trashed()) で囲んだ（F-8 diff 参照）
```
**F-6** — 投稿フォーム非表示＋差し替え文言
証拠:
```
走行⑥ 同テストで ->assertSee('削除済みの書籍にはレビューを投稿できません')  PASS
```
**F-7** — 復元ボタンは本人にのみ表示
証拠:
```
走行⑥ 同テスト（owner視点）で ->assertSee('復元する')  PASS
  ボタンは @can('restore',$book) で囲まれ、BookPolicy::restore は $user->id===$book->user_id && $book->trashed()。
  本人以外では restore が false のため非表示。BookCrudTest::test_restore_authorization で other は403（G-3）。
```
**F-8** — §8-1の4点以外は未変更
証拠:
```
$ git diff 6a346be --name-only -- resources/   （走行①以降に変更されたビュー）
  resources/views/books/show.blade.php   [count=1]  ← show.blade.php 1ファイルのみ

$ git diff 6a346be -- resources/views/books/show.blade.php
（変更は以下4箇所のみ。他のマークアップ・クラス名・レイアウトに差分なし）
 1) session('success')直後: @if($book->trashed()) 「この本は削除されました」バナー追加
 2) お気に入り<!-- お気に入りボタン -->の@auth全体を @if(! $book->trashed()) ... @endif で囲む
 3) 編集・削除の<div>内に @can('restore',$book) の「復元する」PATCHフォームを追加
 4) レビュー投稿の@auth全体を @if(! $book->trashed()) で囲み、@else に
    <p class="mb-6 text-gray-600">削除済みの書籍にはレビューを投稿できません</p> を追加
```

#### G. 復元（restore）
**G-1** — 本人復元→通常状態、一覧・ランキング再表示
証拠:
```
走行⑥ BookCrudTest::test_restore_book  PASS
  owner: patch(books.restore)->assertRedirect(books.show)->assertSessionHas('success','書籍を復元しました')
  復元後 get('/books')->assertSee($title) / get('/ranking')->assertSee($title)
```
**G-2** — 復元後 deleted_at が NULL
証拠:
```
走行⑥ 同テスト: assertNull($book->fresh()->deleted_at)  PASS
```
**G-3** — 本人以外の直URL restore は403
証拠:
```
走行⑥ BookCrudTest::test_restore_authorization  PASS
  other: patch(books.restore)->assertForbidden()（403）、deleted_at は残存
```
**G-4** — 未削除書籍への restore も403
証拠:
```
BookPolicy::restore は $book->trashed() を条件に含む（下記H-3のgrep）。
未削除($book->trashed()===false)では false を返すため、authorize('restore') で403。
（BookCrudTest::test_restore_book は削除済みのみ復元成功を確認）
```

#### H. コード品質・スコープ厳守
**H-1** — pint --test
証拠:
```
$ sail bin pint --test
  PASS ... 107 files
```
**H-2** — バリデーションはFormRequestに分離
証拠:
```
$ grep -c "->validate(|$this->validate(" app/Http/Controllers/*.php app/Http/Controllers/Api/V1/*.php
  no inline validate() found in any controller
BookController::store(StoreBookRequest ...) / update(UpdateBookRequest ...)
```
**H-3** — 認可はBookPolicy、コントローラーで authorize()
証拠:
```
$ grep -n authorize( app/Http/Controllers/BookController.php
  72:  $this->authorize('update', $book);   84: $this->authorize('update', $book);
  99:  $this->authorize('delete', $book);  111: $this->authorize('restore', $book);
$ grep -n trashed app/Policies/BookPolicy.php
  15: return $user->id === $book->user_id && ! $book->trashed();  (update)
  23: return $user->id === $book->user_id && ! $book->trashed();  (delete)
  31: return $user->id === $book->user_id && $book->trashed();    (restore)
（コントローラー内に if($book->user_id!==Auth::id()) 等の手書き判定なし）
```
**H-4** — 発注書外の独自ロジックなし
証拠:
```
BookController は index/create/store/show/edit/update/destroy/restore の8アクションのみ。
追加のバリデーション項目・独自クロージャ・独自画面なし。DB::transaction は発注書§2で指定。
```
**H-5** — migration未変更
証拠:
```
$ git diff 6a346be -- database/migrations/ | wc -l
  0   （走行①完了時点からmigrationの差分なし）
```
**H-6** — QUESTIONS.md
証拠:
```
走行②由来の未解消行なし（QUESTIONS.mdに走行②起因の記録なし）。
```

---

## 走行③ — レビューお気に入りいいね
対象検品表: `docs/03_検品表/03_レビューお気に入りいいね.md`

### 検品前に実行するコマンド（出力）
```
$ git branch --show-current
feature/06-basic-test   （走行③のコミットは 514e4d3）

$ sail artisan route:list --path=reviews
  POST      books/{book}/reviews ...... reviews.store › ReviewController@store
  PUT       reviews/{review} ........ reviews.update › ReviewController@update
  DELETE    reviews/{review} ...... reviews.destroy › ReviewController@destroy
  GET|HEAD  reviews/{review}/edit ....... reviews.edit › ReviewController@edit
  POST      reviews/{review}/like ....... reviews.like › ReviewController@like

$ sail artisan route:list --path=favorites
  POST      books/{book}/favorites favorites.toggle › FavoriteController@toggle
  GET|HEAD  favorites ............ favorites.index › FavoriteController@index

$ sail bin pint --test
  PASS ... 107 files
```

### 行ごとの証拠

#### A. ルーティング
**A-1** — reviews 5本
証拠:
```
route:list --path=reviews に reviews.store/edit/update/destroy/like の5本（上記出力）
```
**A-2** — favorites 2本
証拠:
```
route:list --path=favorites に favorites.index/toggle の2本（上記出力）
```
**A-3** — LikeController なし、like は ReviewController@like
証拠:
```
$ ls app/Http/Controllers/  に LikeController.php は存在しない
route:list: reviews/{review}/like -> ReviewController@like
```
**A-4** — FavoriteController が独立
証拠:
```
app/Http/Controllers/FavoriteController.php が存在（index/toggle）。ReviewControllerとは別クラス。
```
**A-5** — 全7ルートが auth 配下
証拠:
```
routes/web.php: reviews系5本・favorites系2本はいずれも Route::middleware('auth')->group(...) 内に定義。
```

#### B. レビュー投稿・編集・削除
**B-1** — rating必須/comment任意で投稿→books.show→「レビューを投稿しました」
証拠:
```
走行⑥ ReviewTest::test_store_review / test_store_review_without_comment  PASS
  assertRedirect(books.show)->assertSessionHas('success','レビューを投稿しました')
  comment省略時: assertDatabaseHas('reviews',['comment'=>null])
```
**B-2** — rating未入力・範囲外でエラー（§4文言一致）
証拠:
```
走行⑥ ReviewTest::test_store_review_requires_rating: assertSessionHasErrors('rating')  PASS
StoreReviewRequest: rating=required,integer,between:1,5 / messages:
  rating.required=評価を選択してください / rating.between=評価は1〜5の範囲で選択してください
```
**B-3** — 本人は編集・削除可、他人は403
証拠:
```
走行⑥ ReviewTest::test_edit_authorization / test_delete_authorization  PASS
  owner get(reviews.edit)->assertOk / other ->assertForbidden(403)
  other put(reviews.update)->403 / other delete(reviews.destroy)->403
```
**B-4** — 編集画面で $review->book->title 表示（削除済み書籍でも落ちない）
証拠:
```
走行⑥ ReviewTest::test_review_operations_on_deleted_book: 書籍削除後 owner get(reviews.edit)->assertOk()  PASS
Review::book() は belongsTo(Book::class)->withTrashed()（下記 走行⑥ U-R4）
```
**B-5** — レビュー削除で review_likes が cascade 削除
証拠:
```
走行⑥ ReviewTest::test_review_delete_cascades_likes  PASS
  削除前 review_likes count=1 -> destroy 後 count=0
```
**B-6** — 削除後のリダイレクト先が解決される
証拠:
```
ReviewController@destroy: $book = $review->book;（削除前に退避）-> $review->delete() -> redirect(books.show,$book)
走行⑥ test_delete_authorization で assertRedirect(route('books.show',$review->book)) PASS
```

#### C. いいねトグル
**C-1** — ON/OFF が review_likes に反映
証拠:
```
走行⑥ ReviewLikeTest::test_like_toggle_on_off  PASS
  1回目 post(reviews.like)->assertDatabaseHas('review_likes',...) / 2回目 ->assertDatabaseMissing(...)
```
**C-2** — いいねで flash なし
証拠:
```
ReviewController@like: Auth::user()->likedReviews()->toggle($review); return back();（with()なし）
```
**C-3** — 削除済み書籍のレビューでもいいね可
証拠:
```
走行⑥ ReviewLikeTest::test_like_on_deleted_book_review  PASS
  book削除後 post(reviews.like) -> review_likes count=1
```
**C-4** — 未ログインいいねは /login へ
証拠:
```
reviews.like は auth グループ内。未認証POSTは auth ミドルウェアで /login へリダイレクト。
走行⑥ ScreenAccessTest（auth必須リダイレクト）PASS で同ミドルウェア挙動を確認。
```

#### D. お気に入りトグル・一覧
**D-1** — 未登録で押すと favorites にレコード作成
証拠:
```
走行⑥ FavoriteTest::test_toggle_on: post(favorites.toggle)->assertDatabaseHas('favorites',...)  PASS
```
**D-2** — 登録済みで再度押すとレコード削除
証拠:
```
走行⑥ FavoriteTest::test_toggle_off: 事前attach後 post -> assertDatabaseMissing('favorites',...)  PASS
```
**D-3** — お気に入りで flash なし
証拠:
```
走行⑥ FavoriteTest::test_toggle_has_no_flash: post(favorites.toggle)->assertSessionMissing('success')  PASS
FavoriteController@toggle: return back();（with()なし）
```
**D-4** — 未ログインで /favorites は /login へ
証拠:
```
$ curl (guest) /favorites -> HTTP 302  Location: http://localhost:8022/login
走行⑥ FavoriteTest::test_guest_redirected_from_favorites  PASS
```
**D-5** — 一覧は自分の分のみ
証拠:
```
走行⑥ FavoriteTest::test_index_shows_only_own_favorites  PASS
  ->assertSee('マイお気に入り本')->assertDontSee('他人のお気に入り本')
```
**D-6** — 並び順は books.created_at 降順
証拠:
```
FavoriteController@index: Auth::user()->favoriteBooks()->latest('books.created_at')->paginate(10)
```

#### E. 削除済み書籍との関係
**E-1** — 論理削除済みはお気に入り一覧に出ない
証拠:
```
favoriteBooks() の関連先 Book は SoftDeletes を持つため一覧クエリで自動除外。
走行⑥ BookCrudTest::test_deleted_book_excluded_from_listings（一覧系除外）PASS。
```
**E-2** — 書籍削除後も favorites レコード保持
証拠:
```
favorites.book_id は cascadeOnDelete だが SoftDelete下で物理DELETE未発行のため保持（CLAUDE.md§9-4）。
走行② E-4 と同根拠。
```
**E-3** — 復元でお気に入り一覧に再出現
証拠:
```
E-1（SoftDelete標準除外）+ 走行② G-1（restoreでdeleted_at=null）により、復元後は一覧クエリの対象に戻る。
```

#### F. コード品質・スコープ厳守
**F-1** — pint --test
証拠:
```
PASS ... 107 files
```
**F-2** — Store/UpdateReviewRequest に分離
証拠:
```
app/Http/Requests/StoreReviewRequest.php / UpdateReviewRequest.php 存在。ReviewController内に inline validate() なし（走行② H-2 grep）。
```
**F-3** — ReviewPolicy＋authorize()
証拠:
```
$ grep -n authorize( app/Http/Controllers/ReviewController.php
  28: update  40: update  52: delete
ReviewPolicy::update/delete = $user->id === $review->user_id
```
**F-4** — ReviewPolicy に書籍の trashed 判定を含まない
証拠:
```
$ grep -n trashed app/Policies/ReviewPolicy.php
  （一致なし）no trashed() in ReviewPolicy
```
**F-5** — 発注書外の独自ロジックなし
証拠:
```
ReviewController=store/edit/update/destroy/like、FavoriteController=index/toggle のみ。追加ロジックなし。
```
**F-6** — migration未変更
証拠:
```
$ git diff 6a346be -- database/migrations/ | wc -l -> 0
```
**F-7** — QUESTIONS.md
証拠:
```
走行③由来の未解消行なし。
```

---

## 走行④ — ジャンルランキング
対象検品表: `docs/03_検品表/04_ジャンルランキング.md`

### 検品前に実行するコマンド（出力）
```
$ git branch --show-current
feature/06-basic-test   （走行④のコミットは 9bd1ee7）

$ sail artisan route:list --path=genres
  GET|HEAD   genres ................ genres.index › GenreController@index
  POST       genres ................ genres.store › GenreController@store
  GET|HEAD   genres/create ....... genres.create › GenreController@create
  GET|HEAD   genres/{genre} .......... genres.show › GenreController@show
  PUT|PATCH  genres/{genre} ...... genres.update › GenreController@update
  DELETE     genres/{genre} .... genres.destroy › GenreController@destroy
  GET|HEAD   genres/{genre}/edit ..... genres.edit › GenreController@edit

$ sail artisan route:list --path=ranking
  GET|HEAD   ranking ............. ranking.index › RankingController@index

$ sail bin pint --test
  PASS ... 107 files
```

### 行ごとの証拠

#### A. ルーティング
**A-1** — genres 7本
証拠:
```
route:list --path=genres に index/create/store/show/edit/update/destroy の7本（上記）
```
**A-2** — ranking.index 存在、auth なし
証拠:
```
route:list --path=ranking: GET|HEAD ranking -> RankingController@index
$ curl (guest) $B/ranking -> HTTP 200（未ログインで閲覧可＝auth未適用）
```
**A-3** — GenrePolicy 不在
証拠:
```
$ ls app/Policies/
  BookPolicy.php  ReviewPolicy.php   （GenrePolicy.php は存在しない）
```

#### B. ジャンル一覧・詳細
**B-1** — 一覧に書籍数（books_count）
証拠:
```
GenreController@index: Genre::withCount('books')->orderBy('id')->get()
走行⑥ GenreTest::test_genre_screens_render: get(genres.index)->assertOk()->assertSee('技術書')  PASS
```
**B-2** — books_count は生存書籍のみ
証拠:
```
withCount('books') は Book の SoftDeletes 影響を受け、削除済みは数えない。
走行⑥ GenreTest::test_destroy_blocked_when_only_trashed_book_attached が
  「削除済み書籍のみ紐付き」でも削除ブロックされることを確認（books_countは0でもDB制約は残る＝§5仕様）。
```
**B-3** — 詳細に紐づく書籍が10件ページネーション
証拠:
```
GenreController@show: $genre->books()->with('genres')->latest('books.created_at')->paginate(10)
走行⑥ GenreTest::test_genre_screens_render: get(genres.show)->assertOk()->assertSee('ジャンル所属本')  PASS
```
**B-4** — タグの色分け（表示中ジャンル一致）
証拠:
```
genres/show.blade.php は $g->id === $genre->id で色分け。$books に with('genres') を渡している（@show）。
```

#### C. 登録・編集
**C-1** — name未入力でエラー
証拠:
```
走行⑥ GenreTest::test_store_requires_name: post('/genres',[])->assertSessionHasErrors('name')  PASS
```
**C-2** — name重複でエラー
証拠:
```
走行⑥ GenreTest::test_store_unique_name: 既存名で post -> assertSessionHasErrors('name')  PASS
StoreGenreRequest: name.unique=このジャンル名は既に登録されています
```
**C-3** — 所有者概念なし、他ユーザーでも編集可
証拠:
```
走行⑥ GenreTest::test_edit_allowed_for_any_authenticated_user  PASS
  other get(genres.edit)->assertOk / other put(genres.update)->assertRedirect(genres.index)->success
```
**C-4** — 編集時の一意性は自身除外
証拠:
```
UpdateGenreRequest: Rule::unique('genres','name')->ignore($this->route('genre'))
走行⑥ test_edit_allowed_for_any_authenticated_user で自ジャンル更新が成功（一意性で弾かれない）。
```

#### D. 削除制限（二重防御）
**D-1** — 紐付きありで error flash、レコード残存
証拠:
```
走行⑥ GenreTest::test_destroy_blocked_when_books_attached  PASS
  delete(genres.destroy)->assertRedirect(genres.index)->assertSessionHas('error','このジャンルに紐づく書籍が存在するため削除できません')
  ->assertDatabaseHas('genres',['id'=>$genre->id])
```
**D-2** — 紐付きなしで削除成功
証拠:
```
走行⑥ GenreTest::test_destroy_success_when_no_books  PASS
  ->assertSessionHas('success','ジャンルを削除しました')->assertDatabaseMissing('genres',...)
```
**D-3** — アプリ側チェックが withTrashed() 付き（500にならない）
証拠:
```
GenreController@destroy: if ($genre->books()->withTrashed()->exists()) return ...->with('error',...)
走行⑥ GenreTest::test_destroy_blocked_when_only_trashed_book_attached  PASS（削除済み書籍のみ紐付きでも error flash・500なし）
```
**D-4** — 二重防御: 直接delete で QueryException
証拠:
```
$ sail artisan tinker（紐付きありジャンルを直接delete）
  target genre: 小説
  QueryException thrown: SQLSTATE[23000]: Integrity constraint violation: 1451 Cannot delete or update a ...
走行⑥ GenreTest::test_db_constraint_prevents_delete: $this->expectException(QueryException::class); $genre->delete();  PASS
```

#### E. ランキング
**E-1** — 平均評価降順TOP10
証拠:
```
RankingController@index: Book::withAvg('reviews','rating')->withCount('reviews')->whereHas('reviews')
  ->orderByDesc('reviews_avg_rating')->orderBy('id')->limit(10)->get()
走行⑥ RankingTest::test_ranking_ordered_by_avg_rating_desc  PASS（高評価本が先頭）
```
**E-2** — レビュー0件は対象外
証拠:
```
走行⑥ RankingTest::test_ranking_excludes_books_without_reviews  PASS（whereHas('reviews')）
```
**E-3** — 論理削除済みは集計・表示から除外
証拠:
```
走行⑥ RankingTest::test_ranking_excludes_deleted_books: 削除後 get('/ranking')->assertDontSee(削除本)  PASS
```
**E-4** — 復元でランキング再出現
証拠:
```
走行⑥ BookCrudTest::test_restore_book: 復元後 get('/ranking')->assertSee($title)  PASS
```
**E-5** — 未ログインで /ranking 閲覧可
証拠:
```
$ curl (guest) $B/ranking -> HTTP 200
走行⑥ RankingTest::test_ranking_public  PASS
```
**E-6** — 変数名は $rankedBooks
証拠:
```
$ grep -n rankedBooks app/Http/Controllers/RankingController.php
  14: $rankedBooks = Book::withAvg(...   22: return view('ranking.index', compact('rankedBooks'));
```
**E-7** — 平均評価が本文=小数第2位／右バッジ=小数第1位
証拠:
```
ranking/index.blade.php: 本文 number_format($book->reviews_avg_rating,2) / 右バッジ number_format(...,1)
コントローラーは生の平均値を渡し丸めていない（withAvgのreviews_avg_ratingをそのまま）。
実機 $B/ranking のHTML内に両表記が存在（books.show へのリンクとともに描画・HTTP 200）。
```

#### F. コード品質・スコープ厳守
**F-1** — pint --test
証拠:
```
PASS ... 107 files
```
**F-2** — Store/UpdateGenreRequest に分離
証拠:
```
app/Http/Requests/StoreGenreRequest.php / UpdateGenreRequest.php 存在。GenreController内に inline validate() なし。
```
**F-3** — ランキングは withAvg/withCount/whereHas、with('reviews')不使用
証拠:
```
RankingController@index のクエリに with('reviews') は含まれない（上記 E-1 のクエリ全文）。
```
**F-4** — 発注書外の独自ロジックなし
証拠:
```
GenreController=index/create/store/show/edit/update/destroy（resource7）、RankingController=index のみ。GenrePolicy未作成。
```
**F-5** — migration未変更
証拠:
```
$ git diff 6a346be -- database/migrations/ | wc -l -> 0
```
**F-6** — QUESTIONS.md
証拠:
```
走行④由来の未解消行なし。
```

---

## 走行⑤ — 公開APIシーディング
対象検品表: `docs/03_検品表/05_公開APIシーディング.md`

### 検品前に実行するコマンド（出力）
```
$ git branch --show-current
feature/06-basic-test   （走行⑤のコミットは c4fb811）

$ sail artisan migrate:fresh --seed
  INFO  Seeding database.
  UserSeeder .......... DONE / GenreSeeder .......... DONE / BookSeeder .......... DONE
  ReviewSeeder .......... DONE / FavoriteSeeder .......... DONE / ReviewLikeSeeder .......... DONE

（件数、tinker DB::table()->count() で確認）
  users=5  genres=10  books=11  reviews=32  favorites=19  review_likes=48

$ sail artisan route:list --path=api
  GET|HEAD   api/v1/books ..... books.index › Api\V1\BookController@index
  POST       api/v1/books ..... books.store › Api\V1\BookController@store
  GET|HEAD   api/v1/books/{book} books.show › Api\V1\BookController@show
  PUT|PATCH  api/v1/books/{book} books.update › Api\V1\BookController@update
  DELETE     api/v1/books/{book} books.destroy › Api\V1\BookController@destroy
  Showing [5] routes

$ sail bin pint --test
  PASS ... 107 files
```

### 行ごとの証拠

#### A. シーディング（採点直結）
**A-1** — db:seed がエラーなく完了
証拠:
```
migrate:fresh --seed の出力で全6Seederが DONE（上記）。エラー出力なし。
```
**A-2** — users5/genres10/books11/reviews32
証拠:
```
tinker: users=5 genres=10 books=11 reviews=32（favorites=19 review_likes=48）
走行⑥ SeederTest::test_seed_counts: assertSame(5/10/11/32) PASS
```
**A-3** — 2回実行で重複なし（firstOrCreate/syncWithoutDetaching）
証拠:
```
$ sail artisan db:seed（2回目）後 tinker:
  after 2nd seed: users=5 genres=10 books=11 reviews=64
  → users/genres/books は firstOrCreate により不変（5/10/11）。
  → reviews は ReviewSeeder が create 使用（発注書§8-4指定）のため 32→64 に増加する。
    （F-P12 の一意性保証対象は firstOrCreate を使う users/genres/books。reviews の create は仕様どおり）
走行⑥ SeederTest::test_seed_is_idempotent: 2回seed後 users/genres/books=5/10/11 assertSame PASS
```
**A-4** — GenreSeeder投入順・書籍・ユーザーが要件シート シート9と一致
証拠:
```
users（tinker, id昇順）:
  1 山田太郎 yamada@example.com / 2 鈴木花子 suzuki@example.com / 3 田中一郎 tanaka@example.com
  4 佐藤美咲 sato@example.com / 5 高橋健太 takahashi@example.com
genres 投入順=小説,ビジネス,技術書,自己啓発,エッセイ,歴史,科学,芸術,料理,旅行（GenreSeeder配列順、id 1〜10）
books: BookSeeder に11件（吾輩は猫である〜コンテナ物語）を シート9記載のタイトル/著者/ISBN/出版日/ジャンルで定義。
  例: book_id=2「人を動かす」の book_genre = ビジネス / 自己啓発（tinker確認）
```
**A-5** — id=3=リーダブルコード、genre_id=3=技術書
証拠:
```
tinker: book3=リーダブルコード / genre3=技術書
```
**A-6** — description と image_url（?text={番号}）
証拠:
```
tinker（image_url サンプル）:
  1 => https://placehold.co/200x300/e2e8f0/475569?text=1
  3 => https://placehold.co/200x300/e2e8f0/475569?text=3
  11 => https://placehold.co/200x300/e2e8f0/475569?text=11
BookSeeder は各書籍に description 文字列を設定（firstOrCreate の属性）。
```
**A-7** — 各レビューのいいねが自分を除いて0〜3人
証拠:
```
tinker: self likes (review.user_id = review_likes.user_id の件数) = 0
ReviewLikeSeeder: likeCount = reviewIndex % 4（0〜3）、投稿者本人を除外して syncWithoutDetaching。
```

#### B. AP01 書籍一覧
**B-1** — パラメータなしで200、per_page=10
証拠:
```
$ curl $B/api/v1/books
  data_count= 10
  meta= {'current_page':1,'last_page':2,'per_page':10,'total':11, ...}
  HTTP 200
```
**B-2** — 不正パラメータで422（§3文言一致）
証拠:
```
$ curl $B/api/v1/books?per_page=101
  {"message":"入力内容に誤りがあります。","errors":{"per_page":["取得件数は1〜100の範囲で指定してください"]}}  [HTTP 422]
走行⑥ BookApiTest::test_index_validation_error: per_page=101 / page=0 / genre_id=9999 いずれも 422  PASS
```
**B-3** — description を含まない
証拠:
```
$ curl $B/api/v1/books の data[0] キー:
  ['id','title','author','isbn','published_date','image_url','average_rating','reviews_count','genres']
  has_description= False
```
**B-4** — genres/average_rating/reviews_count を含む
証拠:
```
上記 data[0] キーに genres / average_rating / reviews_count が含まれる。
例（genre_id=3）: id=3 average_rating=4 reviews_count=3 genres=[{id:3,name:技術書}]
```
**B-5** — 0件でも data:[] で200
証拠:
```
$ curl $B/api/v1/books?keyword=zzzzzznone
  data= []  [HTTP 200]
```
**B-6** — ページ送りで検索条件維持（withQueryString）
証拠:
```
$ curl $B/api/v1/books?genre_id=3
  links.first= http://localhost:8022/api/v1/books?genre_id=3&page=1   （genre_id が links に引き継がれる）
```

#### C. AP02 書籍詳細
**C-1** — 200、reviews に投稿者名/評価/コメント/投稿日時
証拠:
```
$ curl $B/api/v1/books/3 の data:
  keys= [...,'genres','reviews']
  reviews[0]= {'user_name':'田中一郎','rating':5,'comment':'読み応えがあり、満足しています。','created_at':'2026-09-02T11:05:27+09:00'}
```
**C-2** — comment=null でも key が落ちない
証拠:
```
走行⑥ BookApiTest::test_show_success_with_null_comment  PASS
  comment=null と comment='あり' の2件を作成し assertJsonCount(2,'data.reviews')
ReviewResource は 'comment' => $this->comment（whenLoaded/whenでガードせず常に出力）
```
**C-3** — created_at が ISO8601・+09:00
証拠:
```
上記 C-1: created_at= 2026-09-02T11:05:27+09:00
ReviewResource: 'created_at' => $this->created_at->toIso8601String()
```
**C-4** — 存在しないIDで404（errorsキーなし）
証拠:
```
$ curl $B/api/v1/books/99999
  {"message":"指定された書籍が見つかりません。"}  [HTTP 404]   （errorsキーなし）
```
**C-5** — 論理削除済みIDで404
証拠:
```
AP05 で id=11 削除後: $ curl $B/api/v1/books/11 -> [HTTP 404]
走行⑥ BookApiTest::test_show_soft_deleted_returns_404  PASS
```

#### D. AP03 書籍登録
**D-1** — 201、reviews キーなし
証拠:
```
$ curl -X POST $B/api/v1/books （正常データ）
  data keys= ['id','title','author','isbn','published_date','description','image_url','average_rating','reviews_count','genres']
  has_reviews= False   [HTTP 201]
```
**D-2** — user_id をレスポンスに含まない
証拠:
```
上記 D-1: has_user_id= False（BookResource に user_id キーなし）
```
**D-3** — 422で {message, errors} 形式
証拠:
```
$ curl -X POST $B/api/v1/books -d '{}'
  message= 入力内容に誤りがあります。
  error_fields= ['title','author','isbn','published_date','genres','user_id']   [HTTP 422]
```
**D-4** — genres 配列のみ book_genre に sync
証拠:
```
Api\V1\BookController@store: DB::transaction 内で Book::create -> $book->genres()->sync($request->genres)
走行⑥ BookApiTest::test_store_success_and_validation で 201 + assertDatabaseHas('books',...) PASS
```

#### E. AP04 書籍更新
**E-1** — 200
証拠:
```
$ curl -X PUT $B/api/v1/books/1 （正常データ）
  title= 吾輩は猫である(改)   [HTTP 200]
```
**E-2** — ISBN一意性は自身除外
証拠:
```
上記 E-1 は book1 の元ISBN(9784101010014)のまま更新 -> HTTP 200（自身のISBNで弾かれない）
UpdateApiBookRequest: Rule::unique('books','isbn')->ignore($this->route('book'))
走行⑥ BookApiTest::test_update_success_ignores_own_isbn  PASS
```
**E-3** — 存在しないIDで404、バリデーション違反で422
証拠:
```
$ curl -X PUT $B/api/v1/books/99999 （正常データ）-> [HTTP 404]
$ curl -X PUT $B/api/v1/books/2 -d '{}' -> [HTTP 422]
走行⑥ BookApiTest::test_update_not_found_and_validation  PASS
```

#### F. AP05 書籍削除
**F-1** — 204（本文なし）
証拠:
```
$ curl -X DELETE $B/api/v1/books/11 -> [HTTP 204]
Api\V1\BookController@destroy: $book->delete(); return response()->noContent();
```
**F-2** — 物理削除でなく論理削除（deleted_at）
証拠:
```
削除後 tinker: Book::withTrashed()->find(11)->deleted_at = 2026-09-02 11:06:05
走行⑥ BookApiTest::test_destroy_soft_deletes: assertSoftDeleted('books')+assertDatabaseHas('books') PASS
```
**F-3** — 存在しないIDで404
証拠:
```
$ curl -X DELETE $B/api/v1/books/99999 -> [HTTP 404]
```
**F-4** — 削除後 reviews/favorites/book_genre 保持
証拠:
```
AP05 で id=11 削除後 tinker: reviews_kept=3（book_id=11 のreviews残存）
book_genre/favorites は SoftDelete下で物理DELETE未発行のため保持（CLAUDE.md§9-4）。
```

#### G. コード品質・スコープ厳守
**G-1** — pint --test
証拠:
```
PASS ... 107 files
```
**G-2** — バリデーションは Api\V1 名前空間の FormRequest
証拠:
```
app/Http/Requests/Api/V1/ : ApiFormRequest.php / IndexBookRequest.php / StoreApiBookRequest.php / UpdateApiBookRequest.php
ApiFormRequest::failedValidation で {message:'入力内容に誤りがあります。', errors:...} を422で返す。
```
**G-3** — API側で authorize() を1回も呼ばない
証拠:
```
$ grep -n authorize( app/Http/Controllers/Api/V1/BookController.php
  no authorize() in API controller
```
**G-4** — Web版とAPI版の BookController が別クラスで両方存在
証拠:
```
$ grep ^namespace 各BookController.php
  app/Http/Controllers/BookController.php        : namespace App\Http\Controllers;
  app/Http/Controllers/Api/V1/BookController.php : namespace App\Http\Controllers\Api\V1;
```
**G-5** — 発注書外の独自ロジックなし
証拠:
```
Api\V1\BookController=index/show/store/update/destroy の5アクションのみ。Handler の404差し替えは発注書§7で指定。
```
**G-6** — migration未変更（personal_access_tokens なし）
証拠:
```
$ git diff 6a346be -- database/migrations/ | wc -l -> 0
$ ls database/migrations/ | grep personal_access -> no personal_access_tokens migration
```
**G-7** — QUESTIONS.md
証拠:
```
走行⑤で1件記録:
| 2026-09-02 | 05_公開APIシーディング | Book に published_date の date cast が無い（走行①漏れ）。発注書05§5は
  $this->published_date->format() 前提だが、cast追加は books/show.blade の {{ $book->published_date }} 表示を
  変え frozen Blade 契約（§0）に反する。 | (a)§0優先でモデル未変更・Resource側で Carbon::parse(...)->format('Y-m-d')（採用） (b)モデルに date:Y-m-d cast追加 |
（CLAUDE.md§0・§11に従い記録。採用案(a)で API の published_date は "2012-06-23" 形式で出力＝上記B/C/D/Eの実測どおり）
```

---

## 走行⑥ — 基本テスト
対象検品表: `docs/03_検品表/06_基本テスト.md`

### 検品前に実行するコマンド（出力）
```
$ git branch --show-current
feature/06-basic-test   （走行⑥のコミットは f979e21）

$ sail artisan test
  Tests:    72 passed (223 assertions)
  Duration: 2.65s

$ sail artisan test --coverage
  （抜粋）
  Http/Controllers/BookController .............. 94.3%
  Http/Controllers/Api/V1/BookController ....... 86.1%
  Http/Controllers/GenreController ............. 100.0%
  Http/Controllers/ReviewController ............ 100.0%
  Http/Controllers/FavoriteController .......... 100.0%
  Http/Controllers/RankingController ........... 100.0%
  Http/Resources/*(4本) ........................ 100.0%
  Http/Requests/*(全10本) ...................... 100.0%
  Policies/BookPolicy / ReviewPolicy ........... 100.0%
  Models/Book 75.0% / Genre 100% / Review 100%
  Total: 95.8 %

$ sail bin pint --test
  PASS ... 107 files
```

### 行ごとの証拠

#### A. 実行結果
**A-1** — test 全件（FAIL 0）
証拠:
```
Tests: 72 passed (223 assertions)   （FAIL 行なし）
```
**A-2** — coverage 60%超
証拠:
```
Total: 95.8 %
```
**A-3** — フレーキーでない（2回実行一致）
証拠:
```
run #1: Tests: 72 passed (223 assertions)
run #2: Tests: 72 passed (223 assertions)
```

#### B. テストファイルの存在と観点網羅
**B-1** — ScreenAccessTest（F-A1〜F-A4）
証拠:
```
tests/Feature/ScreenAccessTest.php（4 methods）
  test_authenticated_user_redirected_from_guest_pages(F-A1) / test_guest_redirected_from_auth_pages(F-A2)
  test_public_pages_accessible_by_guest(F-A3) / test_intended_url_after_login(F-A4)
```
**B-2** — AuthTest（F-A5〜F-A9）
証拠:
```
tests/Feature/AuthTest.php（5 methods）
  test_registration_logs_in_and_redirects(F-A5) / test_registration_validation_errors(F-A6)
  test_login_success(F-A7) / test_login_failure_message(F-A8) / test_logout_requires_login_again(F-A9)
  ※F-A8: session('errors')->first('email')==='メールアドレスまたはパスワードが正しくありません' を assert
```
**B-3** — Unit/BookTest（U-B1〜U-B5）
証拠:
```
tests/Unit/BookTest.php（5 methods）
  test_book_belongs_to_user(U-B1) / test_book_belongs_to_many_genres(U-B2) / test_book_has_many_reviews(U-B3)
  test_reviews_avg_rating(U-B4: 0件でnullも確認) / test_soft_delete_exclusion(U-B5)
```
**B-4** — BookCrudTest（F-B1〜F-B13）
証拠:
```
tests/Feature/BookCrudTest.php（13 methods）
  test_store_book_success(F-B1) / test_store_validation_errors(F-B2) / test_genre_sync_on_store_and_update(F-B3)
  test_edit_authorization(F-B4) / test_delete_authorization(F-B5) / test_deleted_book_excluded_from_listings(F-B6)
  test_deleted_book_show_page(F-B7) / test_deleted_book_hides_buttons(F-B8) / test_deleted_book_reviews_still_visible(F-B9)
  test_restore_book(F-B10) / test_restore_authorization(F-B11) / test_deleted_isbn_uniqueness(F-B12)
  test_reviews_kept_after_book_soft_delete(F-B13)
```
**B-5** — Unit/ReviewTest（U-R1〜U-R4）
証拠:
```
tests/Unit/ReviewTest.php（4 methods）
  test_review_belongs_to_book(U-R1) / test_review_belongs_to_user(U-R2)
  test_review_liked_by_users(U-R3) / test_review_book_uses_with_trashed(U-R4)
```
**B-6** — Feature/ReviewTest（F-R1〜F-R5）
証拠:
```
tests/Feature/ReviewTest.php（7 methods）
  test_store_review / test_store_review_without_comment / test_store_review_requires_rating（F-R1）
  test_edit_authorization(F-R2) / test_delete_authorization(F-R3) / test_review_delete_cascades_likes(F-R4)
  test_review_operations_on_deleted_book(F-R5)
```
**B-7** — ReviewLikeTest（F-L1〜F-L2）
証拠:
```
tests/Feature/ReviewLikeTest.php（2 methods）
  test_like_toggle_on_off(F-L1) / test_like_on_deleted_book_review(F-L2)
```
**B-8** — Unit/FavoriteTest（U-F1）
証拠:
```
tests/Unit/FavoriteTest.php（1 method）: test_favorite_books_toggle(U-F1)
```
**B-9** — Feature/FavoriteTest（F-F1〜F-F5）
証拠:
```
tests/Feature/FavoriteTest.php（5 methods）
  test_toggle_on(F-F1) / test_toggle_off(F-F2) / test_toggle_has_no_flash(F-F3)
  test_guest_redirected_from_favorites(F-F4) / test_index_shows_only_own_favorites(F-F5)
```
**B-10** — Unit/GenreTest（U-G1）
証拠:
```
tests/Unit/GenreTest.php（1 method）: test_genre_belongs_to_many_books(U-G1)
```
**B-11** — Feature/GenreTest（F-G1〜F-G6）
証拠:
```
tests/Feature/GenreTest.php（9 methods）
  test_store_requires_name(F-G1) / test_store_unique_name(F-G2) / test_edit_allowed_for_any_authenticated_user(F-G3)
  test_destroy_blocked_when_books_attached(F-G4) / test_destroy_blocked_when_only_trashed_book_attached(F-G4補足)
  test_destroy_success_when_no_books(F-G5) / test_db_constraint_prevents_delete(F-G6)
  （加えて test_genre_screens_render / test_store_success の画面・登録カバー）
```
**B-12** — RankingTest（F-K1〜F-K4）
証拠:
```
tests/Feature/RankingTest.php（4 methods）
  test_ranking_ordered_by_avg_rating_desc(F-K1) / test_ranking_excludes_books_without_reviews(F-K2)
  test_ranking_excludes_deleted_books(F-K3) / test_ranking_public(F-K4)
```
**B-13** — Api/BookApiTest（F-P1〜F-P10）
証拠:
```
tests/Feature/Api/BookApiTest.php（10 methods）
  test_index_success_default_pagination(F-P1) / test_index_validation_error(F-P2)
  test_show_success_with_null_comment(F-P3) / test_show_not_found(F-P4) / test_show_soft_deleted_returns_404(F-P5)
  test_store_success_and_validation(F-P6) / test_update_success_ignores_own_isbn(F-P7)
  test_update_not_found_and_validation(F-P8) / test_destroy_soft_deletes(F-P9) / test_destroy_not_found(F-P10)
```
**B-14** — SeederTest（F-P11〜F-P12）
証拠:
```
tests/Feature/SeederTest.php（2 methods）: test_seed_counts(F-P11) / test_seed_is_idempotent(F-P12)
```

#### C. テストの質
**C-1** — 認可系で「本人」と「それ以外」を別Userで扱う
証拠:
```
BookCrudTest::test_edit_authorization / test_delete_authorization / test_restore_authorization:
  $owner = User::factory()->create(); $other = User::factory()->create();（別インスタンス）
ReviewTest::test_edit_authorization / test_delete_authorization: 同様に $owner と $other を分離。
GenreTest::test_edit_allowed_for_any_authenticated_user: $creator と $other を分離。
```
**C-2** — SoftDeletes系はDBレベル保持を assertDatabaseHas 等で確認
証拠:
```
BookCrudTest::test_reviews_kept_after_book_soft_delete: assertDatabaseHas('reviews',['id'=>$review->id,'book_id'=>$book->id])
BookApiTest::test_destroy_soft_deletes: assertSoftDeleted('books')+assertDatabaseHas('books')
（画面/API除外の assertDontSee/assertJsonMissing と併用）
```
**C-3** — レスポンスボディ構造まで検証（抜き取り3件）
証拠:
```
BookApiTest::test_index_success_default_pagination: assertJsonCount(10,'data')+assertJsonPath('meta.per_page',10)+('meta.total',15)
BookApiTest::test_store_success_and_validation: assertJsonPath('data.title',...)+assertJsonMissingPath('data.reviews')+assertJsonMissingPath('data.user_id')
AuthTest::test_login_failure_message: session('errors')->first('email') の文言一致まで検証
```
**C-4** — 実装不具合の無断修正なし
証拠:
```
走行⑥コミット f979e21 の変更対象:
$ git diff 6a346be --name-status（走行②〜⑥全体）で app/・database/ 側の A/M は走行②〜⑤のコミット群に属する。
走行⑥コミット単体（f979e21）は tests/ 配下の追加のみ（下記 D-2）。実装コードの修正は含まない。
published_date cast の走行①漏れは走行⑤時点で QUESTIONS.md に記録済み（無断修正はしていない）。
```

#### D. コード品質・スコープ厳守
**D-1** — pint --test
証拠:
```
PASS ... 107 files
```
**D-2** — 走行⑥コミットは tests/ 配下のみ
証拠:
```
$ git show --stat f979e21（走行⑥コミット）の変更ファイルは tests/ 配下（Unit/Feature/Api）および
  ExampleTest.php の削除のみ。Controller/Model/Policy/FormRequest/Resource/migration への差分なし。
```
**D-3** — QUESTIONS.md
証拠:
```
走行⑥由来の未解消行なし（走行⑤記録の1件のみ。テスト作成過程で新たな実装不一致は検出せず、全72テストPASS）。
```
