status: issued

# 差し戻し指示 — 走行②③⑤ 残存4件（最終ラウンド）

**根拠**: 検品結果02/03/05（第3回再検品版）、`検品表02_B2・検品表03_C4_修正パッチ.md`
**発行日**: 2026-09-03

---

## 1. ②B-2 ／ ③C-4（検品表の確認方法変更に伴うテスト追加）

**対象**: `tests/Feature/ScreenAccessTest.php`（または`BookCrudTest.php`）／`tests/Feature/ReviewLikeTest.php`
**指示**: `検品表02_B2・検品表03_C4_修正パッチ.md`記載の2テストメソッドをそのまま追加する。
**完了条件**: 両テストがPASSし、実行結果を証拠として貼る。前回貼った`curl`の419出力はそのまま残してよい（CSRF保護が機能している証拠として有効）。

## 2. ③E-2 ／ ⑤F-4（検証手順の修正のみ・コード変更不要）

**対象**: 検証手順のみ（実装・シードは変更不要）
**指示**: カウント前に対象書籍を実際に論理削除してから確認する。
```
sail artisan tinker --execute="
\$book = App\Models\Book::find(11);
\$book->delete();
echo 'deleted_at='.\$book->fresh()->deleted_at.PHP_EOL;
echo 'favorites_count='.DB::table('favorites')->where('book_id',11)->count();
"
```
**完了条件**: `deleted_at`が空でない値になっていることと、`favorites_count`が0でないことの両方が同一出力内に含まれること。この1コマンドの結果を③E-2・⑤F-4双方の証拠として使ってよい。

---

## 完了時の報告

- 上記2件の再提出をもって、走行②③⑤は全行YESが確定する見込み（走行④はすでに全行YES確定済み）。
- `QUESTIONS.md`の該当2行（419の件）は解消扱いにせず、そのまま残してよい（実装判断ではなく検証方法の記録として有用なため）。
