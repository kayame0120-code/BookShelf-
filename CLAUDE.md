# CLAUDE.md — BookShelf 実装・検品 恒久ルール

このファイルはCCが実装セッション・検品セッションどちらでも起動時に自動読込する。
ここに書かれたルールは発注書ごとに転記しない。発注書は機能固有の指示のみを書く。

---

## 0. 正本優先順位

仕様の食い違いが発生した場合、以下の優先順位で判断する。**上位が常に勝つ。**

1. Bladeモック実体（`docs/00_正本/Bladeモック参照.md` 参照。basic/advancedブランチ、frozen）
2. 要件シート.xlsx（`docs/00_正本/要件シート.xlsx`）
3. 01_機能仕様書/ 配下のmd
4. 02_発注書/ 配下のmd

発注書の記述と実際のBladeコードが食い違う場合、Bladeが勝つ。仮決めせず`QUESTIONS.md`へ記録して停止すること（詳細は11章）。

---

## 1. Git運用

- **【重要】機能ごとに必ずブランチを作成してから作業を始めること。mainブランチへの直接コミット（直書き）は禁止。**
- 作業開始前に `git checkout -b <ブランチ名>` を実行してから着手する。mainブランチ上での編集・コミットは一切行わない。
- ブランチ名は何をするブランチか分かる名前にする（例: `feature/book-crud`, `feature/genre-ranking`）。
- 作業完了後、動作確認とPint整形を終えてからmainへマージする。**「動作確認」とは12章の判定でその発注書が全行YESと確定したことを指す。判定前のmainマージは禁止。**
- コミットメッセージは「何をしたか」が明確にわかるように記述する（例: `feat: 書籍一覧表示を実装`）。
- 複数の発注書を1セッションで**まとめて実行するバッチ運用**を取る場合、バッチ内の発注書はブランチを分けなくてよい。1本のブランチ上に発注書ごとの**別コミット**を積み重ねる（例: `feature/02-book-crud`相当のコミット→`feature/03-review-favorite-like`相当のコミット→…と同一ブランチ上で連続コミット）。ブランチ名はバッチの内容・最終工程が分かる名前にする（例: `feature/06-basic-test`のようにバッチの仕上げ工程名を使ってよい）。
- ブランチを分けるべきなのは、バッチ内の一部だけが先にmainへマージされる（判定確定タイミングがバッチ内でずれる）場合、またはバッチをまたいで前のバッチの成果がすでにmainへマージされた後に新しいバッチへ着手する場合のみ。この条件に当てはまらない限り、無理にブランチを分けない。
- **ブランチを分けないことは、コミットをまとめてよいことを意味しない。** コミット単体は発注書ごとに分ける（`git log`で発注書の境界が追える状態を維持する）。
- mainへのマージは、バッチ全体の判定が確定してから1回で行ってよい（バッチ内の発注書を1本ずつ順にマージし直す必要はない）。

---

## 2. アーキテクチャ

本プロジェクトは Traditional Web（Blade + セッション認証）＋ 外部向け公開API（JSON）の構成。

- **Webブラウザ向け機能**: Bladeテンプレート、セッション認証（Cookie）。`routes/web.php` と `app/Http/Controllers/` を使用。
- **公開API（JSON）**: `routes/api.php` と `app/Http/Controllers/Api/V1/` 名前空間を使用。基本機能は認証なしCRUD、応用機能でSanctumトークン認証を後付けする。

---

## 3. 命名規則

Laravel標準命名規則（PSR-12準拠）に従う。

| 対象 | 規則 |
|---|---|
| 変数・メソッド | camelCase |
| クラス | PascalCase |
| DBテーブル | snake_case（複数形） |
| DBカラム | snake_case（単数形） |
| モデル名 | アッパーキャメル |
| コントローラー名 | アッパーキャメル |
| フォームリクエスト名 | アッパーキャメル |
| マイグレーションファイル名 | スネークケース |
| シーディングファイル名 | アッパーキャメル |

- `a`, `x` など意味のない変数名・メソッド名を禁止する。
- ローマ字表記（例: `okyakusama`）など英単語でない命名を禁止する。

---

## 4. コードフォーマット

- Laravel Pintを使用してコードを自動整形すること。
- コミット前に必ず `vendor/bin/pint` を実行し、整形されたコードをコミットする。
- 採点基準: `sail bin pint --test` を実行し、指摘0件（Pintのバージョンにより表示は「No fixable issues were found」または「PASS」）であること。

---

## 5. Eloquent ORM

- DB操作にはEloquentを最大限活用し、クエリビルダや生SQLは原則使用しない。
- N+1問題を避けるため、`with()` メソッドによるEager Loadingを適切に使用する。

---

## 6. コントローラーの責務

- コントローラーはリクエストの受付とレスポンスの返却に専念させる。
- 複雑なビジネスロジックはモデルやサービスクラス（任意）に記述する。

---

## 7. FormRequest

- バリデーションロジックは必ずFormRequestクラスに分離する。

---

## 8. Policy

- 認可処理（リソースの所有者チェック等）は必ずPolicyクラスで実装する。
- コントローラーで `$this->authorize()` を使用してPolicyを適用する。
- BookPolicyには `update` / `delete` に加えて `restore`（本人のみ）を実装する。判定内容は `update` / `delete` と同一（`Auth::id() === $book->user_id`）。

---

## 9. 論理削除（SoftDelete）に関する共通ルール

booksテーブルは物理削除ではなく、SoftDeletesトレイトによる論理削除（`deleted_at`）を採用している。**SoftDeleteの対象はbooksテーブルのみ。** reviews・genres等の他テーブルは通常通り物理削除される（この点を混同しないこと）。

### 9-1. withTrashed() が必要な箇所（既知の全箇所）

Eloquentの標準動作では、`Book::find()` やリレーション経由の `$model->book` を含むあらゆるBookクエリに「`deleted_at`がNULL」というグローバルスコープが自動で効き、削除済みレコードは黙って除外される（`null`が返る、または結果から消える）。これにより「削除済みデータへのアクセスが意図せず失敗する」バグを埋め込みやすい。以下の箇所では `withTrashed()` を明示すること。

| 箇所 | 用途 |
|---|---|
| `BookController::show` | 削除済みでも書籍詳細ページを表示するため |
| 復元ルート（`PATCH /books/{book}/restore`） | 削除済みレコードを解決するため |
| `reading_plans.md` 関連（応用）: `$readingPlan->book` | 削除済みの本に紐づく読書計画を表示するため |
| `reports.md` 関連（応用）: マイ読書レポート `top_rated_books` 集計時の `$review->book` | 削除済みの本へのレビューも一覧・リンクに含めるため |

上記以外の箇所でも、削除済み書籍に関連するデータを扱う機能を実装する際は、同様の見落としがないか都度確認すること。未確認のまま進めず、該当しそうな箇所は`QUESTIONS.md`へ記録してよい。

### 9-2. 標準の除外挙動をそのまま使ってよい箇所

書籍一覧・検索・ランキング・公開API（GET系）は、SoftDeletesの標準除外挙動をそのまま利用してよい。削除済み書籍を除外するための追加コードは不要（`withTrashed()`を使わないことが正しい）。

### 9-3. 削除済み書籍の詳細ページの挙動

- バナー「この本は削除されました」を表示する
- 編集・削除・お気に入りボタン、新規レビュー投稿フォームは非表示にする
- レビュー・評価一覧は本人以外の分も含め、引き続きすべて閲覧できる
- 復元ボタンは登録した本人にのみ表示する
- レビューへの「いいね」、レビュー本人による編集・削除は通常通り操作可能（本の状態と無関係な操作のため）

### 9-4. cascade設定に関する注意

favorites・book_genreのbook_id起点の`cascadeOnDelete()`は設定として残すが、SoftDelete下では書籍の物理DELETEが発行されないため実際には発火しない「死んだ設定」になる。これはバグではなく意図した状態であり、削除しなくてよい。review_likesはreviews起点のcascadeであり、reviewsは引き続き物理削除されるため通常通り機能する。

---

## 10. 設定のハードコーディング禁止

- DB接続情報やAPIキーなどの設定値は、必ず `.env` ファイルで管理する。
- コード内に直接設定値を書き込まない。

---

## 11. 未定義事項に当たった場合（QUESTIONS.mdへの退避）

- 発注書・機能仕様書・このCLAUDE.mdのいずれにも定義がない事項に当たった場合、その場で仮決めしない。
- `QUESTIONS.md` に定義された書式（発生日・対象発注書・止まった箇所・CCの解釈候補）で記録し、該当作業を止める。
- 1発注書につき相談3往復で打ち切り、片倉の裁定を待つ。

---

## 12. 検品ワークフロー（証拠収集とチャット側判定）

走行①までは「別のCCセッションが判定する」方式だったが、走行②以降は**「CCは証拠だけを集め、判定はCCの外（Claudeとのチャット）で行う」**方式に変更する。目的は同じ（実装したCC自身が自分の誤読ごと合格判定してしまう事故の防止）。手段だけを変える。

### 12-1. CCの役割は証拠収集のみ。判定語を書かない

- 実装完了後、CCは対応する検品表の全行について、`docs/04_検品結果/証拠_<対象範囲>.md`（雛形は各`03_検品表/NN.md`の行を機械的に写したもの。片倉から渡される）の空欄を埋める。
- 埋めるのは**生の実行結果のみ**: コマンドの標準出力、DBクエリ結果、HTTPレスポンス、画面で確認した文字列や挙動をそのまま書く。
- **「YES」「PASS」「合格」「正しく動作した」「問題ない」等、判定・評価を意味する語を一切書かない。** 判定は必ずチャット側のClaudeが行う。CCがこれを書いた場合、その報告は無効として扱い、証拠の再提出を求める。
- 確認できなかった項目は空欄のままにする。体裁を整えるための推測・決め打ちの記入をしない。空欄は未確認＝NO扱いになる。

### 12-2. 実装セッションが証拠収集を兼ねてよい

- 検品のためだけに新規セッションを起動する必要はない。実装した同じCCセッションが、実装直後に検品前コマンドを実行し証拠を貼ってよい。
- 12-1の禁止事項（判定語を書かない）を守る限り、実装者自身が証拠を集めても自己レビューバイアスの問題は生じない。判定という行為そのものを実装者から切り離しているため。

### 12-3. 提出・判定・マージの順序

1. CCが証拠ファイルの空欄を埋めて提出する。
2. 片倉がそれをClaude（チャット）にアップロードする。
3. Claudeが発注書・検品表と突き合わせ、行ごとにYES/NO/根拠不足を判定し、`docs/04_検品結果/NN_結果.md`（コミットID・全項目のPASS/FAIL・根拠を含む）を出力する。
4. 全行YESが確定するまで、該当featureブランチをmainへマージしない（1章参照）。1行でもNOがあれば差し戻し、証拠を再収集して2に戻る。

### 12-4. 証拠フォーマット規則の参照

12-1の「判定語を書かない」に加えて、証拠を埋める際は`docs/04_検品結果/証拠フォーマット規則.md`を必ず読み、そこに定義された下記5点に従うこと。

1. 生出力の原則（推論・要約だけで1行を成立させない。コマンド出力は原文のまま貼る）
2. 検品表の「確認方法」列を読み替えない（指定と異なる手段の証拠は無効）
3. HTTPメソッド別に個別確認する（GETの結果でPOST/PUT/DELETE/PATCHを代表させない）
4. コンポーネントをまたいだ転用の禁止（別クラス・別ルートの証拠を流用しない）
5. 網羅性が求められる行は部分サンプルで済ませない

上記を満たさない証拠は、検品セッションで機械的に「根拠不足＝NO」として扱われる。判断に迷う場合は`QUESTIONS.md`へ記録し、独断で証拠の粒度を決めない。

（走行②〜⑥の検品で159行中32行が根拠不足によりNO判定となった事例を受けて追記。2026-09-02）

---

## ★ 応用フェーズ追加ルール（基本機能完走後に適用。基本機能では不要）

| 項目 | 内容 | 備考 |
|---|---|---|
| 型宣言 | 全コントローラ・モデル・FormRequest・Policyのメソッドに引数と戻り値の型を明示的に宣言する（例: `public function index(): View`）。モデルのリレーションメソッドには `HasMany`, `BelongsTo`, `BelongsToMany` 等の具体的な戻り値型を宣言する | 基本機能実装時は不要、応用フェーズ着手時に全メソッドへ追加 |
| PHPDoc | 主要なメソッドに処理内容・引数・戻り値を説明するPHPDocコメントを記述する | |
| Collectionメソッド活用 | `foreach` 等の手続き的ループを避け、`map`, `filter`, `groupBy`, `flatMap` 等を積極的に活用する。宣言的で可読性の高いコードにする | マイ読書レポート機能で特に徹底 |
| 外部API連携 | 外部APIとの通信には `Illuminate\Support\Facades\Http` を使用する | ISBN検索（Google Books API）で使用 |
| Sanctum APIトークン認証 | 公開APIの書き込み系エンドポイント（POST/PUT/DELETE）に Sanctum トークン認証を導入する。`auth:sanctum` ミドルウェアで認証必須化し、BookPolicyで書籍所有者のみ更新・削除できるよう認可する | 基本機能では読み取り＋認証なしCRUD、応用でSanctumを後付け |
| Schedule + Console Command | 応用機能で必要なバッチ処理はLaravelのSchedule + Console Commandで実装する | |
| Database Transaction | 複数SQL文を1単位で扱う処理は `DB::transaction()` で原子化する | 例: 通知バッチでの複数レコード一括生成など、複数テーブルにまたがる登録・更新処理 |
| Notification facade（DatabaseChannel） | 通知機能はLaravel標準のNotification facadeを使用し、DatabaseChannelに保存する | |
| PHP Enum + Eloquent cast | 限定された値の集合はPHP 8.1+ネイティブEnumで定義し、Eloquentの `casts()` でカラムに紐付ける | |

---

## 13. テストカバレッジ

- 基本機能のみ: 60%以上を目標
- 応用機能込み: 80%以上を目標（`sail artisan test --coverage` で確認）

---

## 14. その他の要件遵守

- 開発言語はCOACHTECH教材内の言語を使用する。
- ルーティングは「画面設計（機能仕様書）」の画面定義に従う。システムは「機能要件」の要件に従う。
- 認証・バリデーション等が指定技術以外で実装されていないか常に確認する。

---

## 15. 実装スコープの厳守（定義があるのに逸脱した場合）

11章は「発注書・機能仕様書・CLAUDE.mdのいずれにも定義がない」場合の退避ルール。本章はその逆で、**定義があるのに、それと異なる実装をした・しようとしている**場合の禁止ルール。

- 発注書に明記されていないバリデーションロジック・認証ロジック・独自クロージャ等を追加しない。
- 発注書の指定と異なる実装（例: 独自Validator・独自Requestクラス・独自クロージャによる代替実装）を行う必要が生じた場合は、仮決めせず`QUESTIONS.md`に記録して該当作業を止める。
- 「検品表の番号項目に文言上抵触しないから実装した」という判断は禁止。発注書の明示指定からの逸脱は、それ自体が`QUESTIONS.md`への記録対象である。

（走行①で`Fortify::authenticateUsing()`が発注書§177の明示指定に反して追加され、検品表の番号項目・QUESTIONS.mdのいずれにも捕捉されなかった事例を受けて追記。2026-09-01）
